app.put('/users/:id', async (req, res) => { const id = req.params.id; await db.updateUser(id, req.body); // Invalidate cached user and related views await redis.del('user:v1:' + id); await redis.del('user:orders:v1:' + id); return res.status(204).send();
});