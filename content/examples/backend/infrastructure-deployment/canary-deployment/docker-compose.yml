services:
  stable:
    image: example/app:stable
  canary:
    image: example/app:canary
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - '8081:80'