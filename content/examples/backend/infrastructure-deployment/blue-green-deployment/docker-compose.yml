services:
  blue:
    image: example/app:blue
  green:
    image: example/app:green
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - '8080:80'