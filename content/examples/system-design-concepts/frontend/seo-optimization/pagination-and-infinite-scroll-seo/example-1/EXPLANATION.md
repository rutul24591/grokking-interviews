# Example 1 — Crawlable pagination + infinite-scroll enhancement (end-to-end)

Demonstrates:

- server-rendered, crawlable pagination with `<a href>` links
- optional client-side “load more” that does not break indexing
- canonical rules per page

