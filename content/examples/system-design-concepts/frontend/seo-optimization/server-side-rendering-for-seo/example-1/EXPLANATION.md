# Example 1 — SSR with metadata from server data (end-to-end)

Demonstrates:

- server-rendered product pages whose primary content is present in the initial HTML
- `generateMetadata()` using server-side data for title/description/canonical
- a small API surface for the same data (useful for clients, but SEO relies on SSR HTML)

