# Example 3 — Advanced: multi-tenant robots rules

Multi-tenant apps often need:

- tenant-specific allow/disallow rules
- consistent rules under `/tenant/{id}/...`

This example demonstrates generating different policies per tenant context.

