# Example 1 — Next.js `app/robots.ts` with environment gating (end-to-end)

Demonstrates:

- `/robots.txt` generated by Next.js
- different policies for prod vs non-prod (staging should usually disallow indexing)

