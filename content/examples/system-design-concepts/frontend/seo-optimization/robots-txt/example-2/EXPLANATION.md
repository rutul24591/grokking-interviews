# Example 2 — Allow/deny patterns (focused)

Robots rules often need explicit patterns for:

- internal search pages
- user drafts / private pages
- API endpoints

This example builds a policy helper to keep rules consistent and easy to audit.

