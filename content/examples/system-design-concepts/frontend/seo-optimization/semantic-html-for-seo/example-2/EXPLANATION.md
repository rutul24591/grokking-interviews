# Example 2 — Heading hierarchy + TOC generation (focused)

Demonstrates:

- enforcing a consistent heading strategy from a “design system” component (`<Section />`)
- generating a semantic table of contents (`<nav aria-label="Table of contents">`)
- preventing common SEO / extraction failures: skipped heading levels, repeated h1s, untitled sections

