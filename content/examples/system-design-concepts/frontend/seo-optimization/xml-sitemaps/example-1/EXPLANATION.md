# Example 1 — Next.js `app/sitemap.ts` (end-to-end)

Demonstrates the built-in Next.js sitemap integration:

- generates `/sitemap.xml` from a simple content registry
- includes `lastModified` to support crawlers efficiently

