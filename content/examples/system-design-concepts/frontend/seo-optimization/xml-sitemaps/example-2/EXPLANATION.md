# Example 2 — Sitemap index + splitting (focused)

Large sites need splitting:

- a sitemap is limited in URL count (commonly 50k) and size
- use a sitemap index that references multiple sitemap files

This example implements `/sitemap.xml` as an index that links to `/sitemaps/posts-1.xml` and `/sitemaps/posts-2.xml`.

