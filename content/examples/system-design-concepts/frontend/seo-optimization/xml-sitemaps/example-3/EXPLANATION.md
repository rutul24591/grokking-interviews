# Example 3 — Locale-aware URLs (advanced)

Advanced sitemaps include:

- alternate language URLs (hreflang at the page level)
- consistent canonical host selection

This example shows a multi-locale URL list strategy and highlights the need to keep hosts consistent.

