# Example 2 — Title/description templates + truncation (focused)

SEO tags have practical constraints:

- titles and descriptions must be consistent and brand-safe
- some platforms truncate; you should truncate intentionally
- templates must avoid duplicate titles across pages

This example builds a small SEO text utility and lets you preview the output.

