# Example 3 — Locale alternates + metadata correctness (advanced)

Advanced meta tag setups must handle:

- multiple locales (`/en/*`, `/fr/*`)
- correct `hreflang` alternates
- stable canonical URLs per locale

This example demonstrates locale-aware metadata with `alternates.languages`.

