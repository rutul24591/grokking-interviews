# Meta Tags — Example 1

Run:
```bash
pnpm install
pnpm dev
```

Then open:
- `http://localhost:3000/products/p_101`

Inspect the rendered HTML head (View Source) to confirm meta tags are present server-side.

