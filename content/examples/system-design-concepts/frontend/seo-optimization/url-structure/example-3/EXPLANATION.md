# Example 3 — Advanced: locale-prefixed URLs + alternates

Internationalized sites often use:

- locale prefixes (`/en/...`, `/fr/...`)
- stable canonical rules per locale
- language alternates for discoverability

