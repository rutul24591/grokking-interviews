# Example 2 — Social metadata defaults + validation (focused)

This example focuses on a smaller problem:

- build a single function that produces consistent Open Graph / Twitter metadata
- enforce safe defaults (card type, fallbacks)
- keep the calling code minimal and uniform across routes

