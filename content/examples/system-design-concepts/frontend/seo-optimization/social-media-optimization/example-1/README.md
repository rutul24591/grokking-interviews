# Social Media Optimization — Example 1

Run:
```bash
pnpm install
pnpm dev
```

Open:
- `http://localhost:3000/posts/edge-cache`

Then open the OG image URL from the meta tags, e.g.:
- `http://localhost:3000/api/og?title=Edge%20Cache`

