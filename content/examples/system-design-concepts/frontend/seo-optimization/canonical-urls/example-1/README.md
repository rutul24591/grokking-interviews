# Canonical URLs — Example 1

Run:
```bash
pnpm install
pnpm dev
```

Try:
- `http://localhost:3000/products?utm_source=twitter&sort=price`

You should get redirected to the canonical URL without `utm_*`.

