# Example 1 — Canonicalization middleware + canonical meta (end-to-end)

Demonstrates:

- redirecting to a canonical URL (301) by stripping `utm_*` params
- setting `alternates.canonical` via `generateMetadata()`

This reduces duplicate content and improves cache hit rates.

