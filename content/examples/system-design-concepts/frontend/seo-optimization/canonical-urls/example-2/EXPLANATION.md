# Example 2 — Faceted navigation: canonical + noindex (focused)

Faceted navigation creates many URL variants:

- `?color=red&size=m&sort=price`

Often you want:

- a canonical that collapses to the indexable version
- `noindex,follow` for filter permutations to prevent duplicate-content bloat

