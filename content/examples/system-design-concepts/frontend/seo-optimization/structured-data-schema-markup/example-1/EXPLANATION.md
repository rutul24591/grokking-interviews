# Example 1 — JSON-LD for Article + BreadcrumbList (end-to-end)

Demonstrates:

- composing JSON-LD objects (Article + BreadcrumbList)
- embedding them safely via `<script type="application/ld+json">`
- stable URLs and identifiers (important for rich results)

