# Route guards workbench

## Run

1. Install dependencies with    `npm install` or `pnpm install`
2. Start the API server with    `npm run api`
3. In a second terminal start Next.js with    `npm run dev`
