## Focus: choosing the right control

Use `CSP frame-ancestors` for:
- allowlisting embed partners
- more precise policy

Use `X-Frame-Options` as a legacy fallback:
- `DENY` or `SAMEORIGIN` (no allowlist)

