## Token Refresh — Example 3: Rotation + replay detection (simulation)

This script simulates a rotating refresh token family:
- old refresh token is marked “used”
- reuse is treated as a replay signal

### Run
```bash
pnpm i
pnpm start
```

