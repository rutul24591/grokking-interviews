## Input Validation — Example 3: Prototype pollution via unsafe merges

This example demonstrates an advanced edge case:
- merging untrusted objects without filtering dangerous keys like `__proto__`

### Run
```bash
pnpm i
pnpm start
```

