## Authentication Patterns — Example 2: JWT validation essentials (HMAC demo)

This Node.js script demonstrates a focused sub-problem: validating a JWT-like token:
- signature
- `exp` (expiry)
- `iss` / `aud` checks

### Run
```bash
pnpm i
pnpm start
```

