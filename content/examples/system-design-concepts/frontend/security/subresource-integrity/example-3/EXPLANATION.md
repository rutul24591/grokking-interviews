## Why manifests help

If you have many third-party assets, a manifest:
- avoids hardcoding hashes in multiple places
- makes upgrades a single change
- supports multi-env rollouts (staging vs prod assets)

