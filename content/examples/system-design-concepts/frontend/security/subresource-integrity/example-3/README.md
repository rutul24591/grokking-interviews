## SRI — Example 3: Generate an integrity manifest (advanced workflow)

This script demonstrates generating an `asset -> integrity` manifest you can use in templates.

### Run
```bash
pnpm i
pnpm start
```

