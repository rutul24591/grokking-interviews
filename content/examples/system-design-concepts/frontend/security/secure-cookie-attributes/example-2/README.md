## Secure Cookie Attributes — Example 2: Prefix rules (`__Host-`, `__Secure-`)

This script validates whether a cookie name + attributes meet the prefix rules.

### Run
```bash
pnpm i
pnpm start
```

