## RBAC — Example 3: Resource-scoped roles (role bindings)

This script demonstrates a more scalable RBAC approach:
- role bindings per resource (project/org)
- avoids creating global roles for every variation

### Run
```bash
pnpm i
pnpm start
```

