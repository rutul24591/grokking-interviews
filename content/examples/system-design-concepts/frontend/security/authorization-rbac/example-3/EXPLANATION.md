## Why “role explosion” happens

Global roles become unmanageable when permissions differ by resource:
- “project admin”, “project editor”, “org admin”, etc.

Role bindings attach a role to a user *for a specific resource*.

