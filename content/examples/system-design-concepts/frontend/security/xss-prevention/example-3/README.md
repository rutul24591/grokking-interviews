## XSS Prevention — Example 3: Safe URL handling (block `javascript:`)

This example demonstrates an advanced edge case: XSS via URL injection (e.g., `<a href="javascript:...">`).

### Run
```bash
pnpm i
pnpm start
```

