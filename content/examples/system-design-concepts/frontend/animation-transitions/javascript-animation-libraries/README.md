# JavaScript Animation Libraries

- `example-1`: Next.js library selection console for route complexity, orchestration depth, and bundle sensitivity.
- `example-2`: Node scoring model for choosing a library.
- `example-3`: Edge-case runner for SSR, spring tuning, and interruption behavior.
