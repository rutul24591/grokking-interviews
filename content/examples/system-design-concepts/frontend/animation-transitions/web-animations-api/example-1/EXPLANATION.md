# Web Animations API — Example 1

This app behaves like a small WAAPI operations console. It loads different motion presets, exercises play/pause/reverse controls, and exposes lifecycle outcomes such as cancel versus finish so teams can understand where native animation control is sufficient without a library.
