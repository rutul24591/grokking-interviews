# Example 1 — CSS Transitions and Animations

Run with:

```bash
npm install
npm run dev
```

Open the app and switch between motion presets to review enter/exit timing, easing, and reduced-motion behavior.
