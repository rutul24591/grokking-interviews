## Periodic Background Sync — Example 1: Register when possible, fall back when not

### Run
```bash
pnpm i
pnpm build
pnpm start
```

Open `http://localhost:3000`.

### What to try
- See whether the browser reports `periodic-sync` or fallback mode.
- Switch tabs away and back to trigger the refresh-on-visible fallback.

