## Offline-first — Example 3: IndexedDB schema migrations (v1 → v2)

### Run
```bash
pnpm i
pnpm dev
```

Open `http://localhost:3000` and:
1. Click “Seed v1 data”
2. Click “Open as v2 (migrate)”
3. Click “List rows” and confirm rows now have `createdAt`

