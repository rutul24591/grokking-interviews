# Conflict Resolution — Example 2

This example focuses on the merge algorithm itself.

The useful interview point is not just the code path; it is the classification:
- unchanged
- only local changed
- only server changed
- both changed to same value
- both changed differently (conflict)

