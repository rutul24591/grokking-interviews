## Conflict Resolution — Example 1: Merge workbench (Next.js)

### Run
```bash
pnpm i
pnpm build
pnpm start
```

Open `http://localhost:3000`.

### What to try
- Edit local and server on different fields to see automatic merge.
- Edit the same field differently on both sides to surface a manual conflict.

