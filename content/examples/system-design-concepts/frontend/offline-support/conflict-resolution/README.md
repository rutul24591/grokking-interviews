# Conflict Resolution for Offline Changes — Examples

This topic has three runnable examples:

- `example-1`: Next.js merge workbench (ancestor + local + server + conflict surfacing)
- `example-2`: Deterministic three-way merge runner (Node)
- `example-3`: Version vector concurrency detector (Node)

Each example folder contains its own `README.md` with run instructions.

