# CDN Caching — Example 1

This app now demonstrates an actual CDN operations flow:

- compare browser and edge cache policy per route
- identify personalized routes that must bypass shared caches
- simulate surrogate-key purges and see their blast radius

That is substantially closer to how teams operate CDN caching in production.
