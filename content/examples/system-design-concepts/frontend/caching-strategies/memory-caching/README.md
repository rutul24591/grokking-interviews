# Memory Caching Examples

- `example-1`: Next.js in-memory cache dashboard for repeated computations and short-lived reads.
- `example-2`: TTL/LRU cache runner.
- `example-3`: leak and stale-reference safety checks.
