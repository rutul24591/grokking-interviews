# Browser Caching — Example 1

This app demonstrates browser cache behavior with long-lived immutable assets, HTML that always revalidates, and API responses that use ETag-driven conditional requests.
