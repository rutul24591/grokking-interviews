# Custom dimensions and metrics

## Run

1. Install dependencies with `pnpm install`.
2. Start the Next.js app and Node API with `pnpm dev`.
3. Open `http://localhost:3000`.

The UI talks to the topic-specific analytics API on `http://localhost:4513`.
