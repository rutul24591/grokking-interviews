# Example 2 — Auto-pick readable text color (focused)

Demonstrates:

- a small helper that selects black/white text for an arbitrary background
- practical use in component APIs (`Button({ bg })`)
- why this is still not a substitute for token-level contrast governance

