# Example 1 — Contrast checker + tokenized theme (end-to-end)

Demonstrates:

- computing WCAG contrast ratio for foreground/background colors
- showing pass/fail for normal vs large text thresholds
- applying tokenized colors (CSS variables) to keep contrast consistent at scale

