# Example 3 — Forced-colors / high-contrast modes (advanced)

Demonstrates:

- detecting high-contrast contexts (`forced-colors`, `prefers-contrast`)
- applying safe fallbacks with CSS media queries
- why design systems should support these modes explicitly

