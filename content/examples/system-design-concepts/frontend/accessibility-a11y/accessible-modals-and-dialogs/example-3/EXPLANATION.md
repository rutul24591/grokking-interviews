# Example 3 — `alertdialog` + destructive confirmations (advanced)

Demonstrates:

- destructive confirmation using `role="alertdialog"`
- initial focus on the least destructive action (Cancel)
- why not to stack modals (and how to avoid it)

