# Example 1 — Native `<dialog>` modal (end-to-end)

Demonstrates:

- using the native `<dialog>` element with `showModal()` (built-in focus trapping + Escape)
- labeling the dialog with `aria-labelledby` / `aria-describedby`
- restoring focus to the trigger on close

