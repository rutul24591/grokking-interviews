# Example 2 — Custom dialog focus trap + inert background (focused)

Demonstrates:

- a lightweight focus trap for non-`<dialog>` overlays
- using `inert` (with an `aria-hidden` fallback) to hide background content from AT
- Escape + click-outside close behaviors

