# Example 2 — Outline + landmark inspector (focused)

Demonstrates:

- extracting headings (`h1..h6`) and common landmarks from the DOM
- using semantics to build an “outline” without relying on CSS classes
- a dev-only pattern for catching regressions when refactoring component libraries

