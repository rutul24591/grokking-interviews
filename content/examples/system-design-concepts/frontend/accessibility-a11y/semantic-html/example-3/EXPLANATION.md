# Example 3 — Interactive semantics (advanced / edge cases)

Demonstrates:

- correct semantics for interactive UI: `<button>` for actions and `<a>` for navigation
- why `div role="button"` is a footgun (you must re-implement keyboard semantics)
- using native controls to inherit correct focus, name, and keyboard behavior

