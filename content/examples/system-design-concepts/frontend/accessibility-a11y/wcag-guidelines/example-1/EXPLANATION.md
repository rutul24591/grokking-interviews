# Example 1 — “WCAG as checks” audit panel (end-to-end)

Demonstrates:

- translating WCAG-inspired requirements into concrete automated checks
- running checks against a UI subtree and showing violations
- toggling between a compliant and non-compliant variant to see the rule output

