# WCAG Guidelines — Example 3

Run (gate passes/fails depending on input):
```bash
pnpm install
pnpm start
```

Edit:
- `audit-results.json` (simulate improving issues)
- `policy.json` (tighten thresholds)

