# Example 3 — Release gate from an a11y report (advanced)

Demonstrates:

- treating accessibility as a “budget” for release gating
- failing CI when errors exceed thresholds
- producing machine-readable audit output that can be tracked over time

