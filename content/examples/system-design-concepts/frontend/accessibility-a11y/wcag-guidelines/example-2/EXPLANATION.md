# Example 2 — Design-system “policy as code” over component inventory (focused)

Demonstrates:

- representing UI as a small inventory model (buttons, inputs, images)
- evaluating rules as pure functions (no DOM dependency)
- exporting a JSON report that can be used by CI/release gates

