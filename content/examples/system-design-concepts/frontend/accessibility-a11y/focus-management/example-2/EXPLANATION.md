# Example 2 — Focus restore + autofocus hooks (focused)

Demonstrates:

- capturing the previously focused element before opening an overlay
- restoring focus on close (critical for keyboard users)
- a safe `autoFocus` pattern that doesn’t steal focus unexpectedly

