# Example 1 — Route-level focus management (end-to-end)

Demonstrates:

- moving focus to the main page heading on navigation (helps screen reader + keyboard users)
- a polite live-region announcement for “page changed”
- skip link + consistent landmarks (`header/nav/main`)

