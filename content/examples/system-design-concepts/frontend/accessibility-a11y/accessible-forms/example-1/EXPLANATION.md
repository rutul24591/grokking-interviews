# Example 1 — Accessible form with error summary (end-to-end)

Demonstrates:

- visible labels + `htmlFor` wiring
- field help + error text with `aria-describedby`
- `aria-invalid` only when invalid
- an error summary that receives focus and links to invalid fields
- grouping related controls with `<fieldset>` / `<legend>`

