# Example 1 — Accessible Tabs with ARIA (end-to-end)

Demonstrates:

- `role="tablist"`, `role="tab"`, `role="tabpanel"`
- `aria-selected`, `aria-controls`, `aria-labelledby`
- keyboard behavior (ArrowLeft/ArrowRight, Home/End) per WAI-ARIA practices

