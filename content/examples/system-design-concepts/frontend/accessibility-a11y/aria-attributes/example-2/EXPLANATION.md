# Example 2 — Wiring `aria-labelledby` / `aria-describedby` (focused)

Demonstrates:

- consistent ID generation for a field: label, help text, and error text
- using `aria-describedby` to link help + error messages
- choosing between visible label vs `aria-label` (prefer visible labels)

