# Example 3 — `aria-expanded` + `aria-controls` patterns (advanced)

Demonstrates:

- disclosure button using `aria-expanded` and `aria-controls`
- menu button semantics with `aria-haspopup="menu"`
- edge cases: focus return on close, Escape handling, and click-outside behavior

