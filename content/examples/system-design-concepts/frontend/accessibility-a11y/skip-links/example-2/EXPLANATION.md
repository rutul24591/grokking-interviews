# Example 2 — Reusable skip-link component (focused)

Demonstrates:

- a `SkipLink` component that focuses the target reliably
- applying `scrollMarginTop` for sticky headers
- keeping skip-link styling consistent across an app shell

