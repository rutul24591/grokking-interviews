# Example 1 — Skip links + focus target (end-to-end)

Demonstrates:

- a visible-on-focus “Skip to content” link (and additional skip targets)
- making the target focusable (`tabIndex={-1}`) so keyboard users land inside main content
- a small hash-focus helper to ensure focus actually moves after clicking a skip link

