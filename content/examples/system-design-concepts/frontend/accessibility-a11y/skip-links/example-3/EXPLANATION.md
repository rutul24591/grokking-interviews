# Example 3 — Skip targets in SPAs + sticky headers (advanced)

Demonstrates:

- combining “focus main heading on route change” with skip targets
- sticky header offset via `scroll-margin-top`
- avoiding smooth-scroll focus bugs (keep focus deterministic)

