# Example 2 — Visually hidden text + accessible names (focused)

Demonstrates:

- a `VisuallyHidden` component (`sr-only`) for accessible names
- when to use visible text vs `aria-label` vs hidden text
- icon-only buttons with meaningful accessible names

