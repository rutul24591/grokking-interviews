# Example 3 — Loading states: aria-busy + update announcements (advanced)

Demonstrates:

- marking a region as `aria-busy` during async updates
- announcing “results updated” without moving focus
- avoiding re-announcing entire lists (announce summary instead)

