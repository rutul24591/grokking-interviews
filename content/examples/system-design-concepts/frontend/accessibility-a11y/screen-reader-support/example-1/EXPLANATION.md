# Example 1 — Live regions for screen readers (end-to-end)

Demonstrates:

- `role="status"` + `aria-live="polite"` for non-urgent updates
- `role="alert"` for urgent errors
- a small “announcer” that queues messages to avoid overwriting announcements

