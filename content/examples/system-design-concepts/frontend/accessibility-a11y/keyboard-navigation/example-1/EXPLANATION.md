# Example 1 — Roving tabindex toolbar (end-to-end)

Demonstrates:

- roving tabindex (one item tabbable, others focusable via arrow keys)
- a toolbar with ArrowLeft/ArrowRight and Home/End support
- scalable patterns for design systems (button groups, segmented controls)

