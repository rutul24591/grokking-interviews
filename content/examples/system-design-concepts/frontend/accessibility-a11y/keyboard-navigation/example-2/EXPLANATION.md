# Example 2 — `useRovingFocus` hook (focused)

Demonstrates:

- a reusable hook for roving tabindex across a list of focusable elements
- centralizing key handling to avoid duplicating logic across components

