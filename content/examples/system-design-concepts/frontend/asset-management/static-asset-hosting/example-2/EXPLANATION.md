# Example 2 — Signed URL primitives (focused)

This example isolates the core building block:

- canonical request string
- HMAC signature
- expiry window
- timing-safe comparison

You can reuse the same primitive for:

- object storage signed URLs
- CDN signed URLs
- private image/document access

