# Image Optimization & Formats — Example 1

Run:
```bash
pnpm install
pnpm dev
```

Open `http://localhost:3000` and inspect the Network panel:
- different formats (`image/avif`, `image/webp`, `image/png`)
- different widths based on viewport + `sizes`

