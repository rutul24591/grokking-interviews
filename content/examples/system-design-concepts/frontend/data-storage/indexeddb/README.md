# IndexedDB - Examples

This topic has three runnable examples:

- `example-1`: Next.js notes catalog with IndexedDB CRUD and indexed queries
- `example-2`: Minimal typed IndexedDB helper
- `example-3`: Schema upgrade and blocked-version handling

Each example folder contains its own `README.md` with run instructions.

