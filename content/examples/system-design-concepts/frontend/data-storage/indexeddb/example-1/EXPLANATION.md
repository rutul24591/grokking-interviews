# IndexedDB Example 1

IndexedDB is the right browser storage primitive for larger, structured offline datasets. This example shows:

- object store creation
- indexed lookup by tag
- async CRUD with transactions

