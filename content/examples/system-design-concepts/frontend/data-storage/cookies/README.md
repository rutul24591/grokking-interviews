# Cookies - Examples

This topic has three runnable examples:

- `example-1`: Next.js server-readable preference and session cookies
- `example-2`: Safe cookie serialize/parse utility
- `example-3`: Cookie size limits and chunking strategy

Each example folder contains its own `README.md` with run instructions.

