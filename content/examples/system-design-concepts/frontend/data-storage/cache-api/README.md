# Cache API - Examples

This topic has three runnable examples:

- `example-1`: Next.js browser cache panel using the Cache API
- `example-2`: Cache strategy simulator
- `example-3`: Versioned cache invalidation flow

Each example folder contains its own `README.md` with run instructions.

