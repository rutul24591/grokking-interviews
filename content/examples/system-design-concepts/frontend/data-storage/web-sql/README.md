# Web SQL - Examples

This topic has three runnable examples:

- `example-1`: Next.js deprecation and fallback explainer
- `example-2`: Compatibility detection demo
- `example-3`: Migration path from Web SQL rows to IndexedDB records

Each example folder contains its own `README.md` with run instructions.

