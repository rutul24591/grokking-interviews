# File System Access API - Examples

This topic has three runnable examples:

- `example-1`: Next.js browser file editor with feature detection
- `example-2`: Adapter showing supported vs fallback flows
- `example-3`: Permission loss and autosave snapshot handling

Each example folder contains its own `README.md` with run instructions.

