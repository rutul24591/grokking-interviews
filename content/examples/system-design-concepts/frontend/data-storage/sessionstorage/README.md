# SessionStorage - Examples

This topic has three runnable examples:

- `example-1`: Next.js multi-step flow using tab-scoped recovery
- `example-2`: Session-scoped helper with fallback semantics
- `example-3`: Tab isolation and opener behavior demonstration

Each example folder contains its own `README.md` with run instructions.

