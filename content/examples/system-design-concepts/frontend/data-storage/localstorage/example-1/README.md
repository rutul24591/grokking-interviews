## LocalStorage - Example 1: Draft + preference persistence

### Run
```bash
pnpm i
pnpm dev
```

Open `http://localhost:3000`, type in the draft area, refresh, and verify the draft and preferences survive.

