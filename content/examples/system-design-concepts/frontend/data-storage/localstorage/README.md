# LocalStorage - Examples

This topic has three runnable examples:

- `example-1`: Next.js draft and preference persistence with SSR-safe hydration
- `example-2`: Typed localStorage wrapper with TTL and quota handling
- `example-3`: Migration and corruption recovery strategy

Each example folder contains its own `README.md` with run instructions.

