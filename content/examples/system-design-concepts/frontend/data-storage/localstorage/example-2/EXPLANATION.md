# LocalStorage Example 2

This wrapper shows the minimum production additions teams usually want:

- namespacing
- safe JSON parse
- TTL-based expiry

The example uses an in-memory storage adapter so it is runnable in Node while keeping the same API shape as localStorage.

