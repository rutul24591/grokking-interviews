# Example 2 — Defer Heavy Widgets (Dynamic Import)

## Run it (after copy-paste)
```bash
pnpm install
pnpm dev
```

Open:
- `http://localhost:3000`

Click **Open editor** to load and mount the heavy island.

