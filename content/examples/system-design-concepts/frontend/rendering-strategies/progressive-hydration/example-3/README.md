# Example 3 — Progressive Hydration: Scheduling, Cancellation, Deduplication

## Run it (after copy-paste)
```bash
pnpm install
pnpm dev
```

Open:
- `http://localhost:3000`

Observe idle-loaded islands and use **Load now** to preempt/cancel background scheduling.

