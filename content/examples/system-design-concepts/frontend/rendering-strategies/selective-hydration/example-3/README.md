# Example 3 — Boundary Placement: Coarse vs Fine

## Run it (after copy-paste)
```bash
pnpm install
pnpm dev
```

Compare:
- `http://localhost:3000/?mode=coarse`
- `http://localhost:3000/?mode=fine`

In `fine`, the Search box should hydrate immediately while the Comments island hydrates later.

