"use client";
import { useMemo, useState } from "react";
export function ProgressiveEnhancementClient() {
  const [saved, setSaved] = useState("No draft submitted");
  const enhanced = useMemo(() => typeof window !== 'undefined' && 'FormData' in window, []);
  return <main className="mx-auto min-h-screen max-w-5xl p-8"><section className="rounded-3xl border border-slate-800 bg-slate-950/70 p-6"><p className="text-sm uppercase tracking-[0.25em] text-emerald-300">Progressive enhancement</p><h1 className="mt-2 text-3xl font-semibold">Baseline article form</h1><p className="mt-2 max-w-3xl text-sm text-slate-400">The form works as plain HTML first. Extra browser capabilities only add autosave, inline hints, and richer recovery.</p><form className="mt-6 space-y-3" onSubmit={(event) => { event.preventDefault(); setSaved(enhanced ? "Draft saved with enhanced validation and autosave metadata." : "Draft submitted through baseline HTML flow."); }}><input className="w-full rounded-2xl border border-slate-700 bg-slate-900 px-4 py-3" placeholder="Article title" /><textarea className="w-full rounded-2xl border border-slate-700 bg-slate-900 px-4 py-3" placeholder="Summary" /><div className="flex flex-wrap gap-3"><button className="rounded-2xl bg-emerald-400 px-4 py-3 font-medium text-slate-950" type="submit">Save draft</button><button className="rounded-2xl border border-slate-700 px-4 py-3" type="button" onClick={() => setSaved("Autosave checkpoint stored locally.")} disabled={!enhanced}>Simulate autosave</button></div></form><div className="mt-6 grid gap-4 lg:grid-cols-2"><article className="rounded-2xl border border-slate-800 bg-slate-900/60 p-4 text-sm text-slate-300">{enhanced ? 'Enhanced mode: autosave and inline validation available.' : 'Baseline mode: full-page form submit still works.'}</article><article className="rounded-2xl border border-slate-800 bg-slate-900/60 p-4 text-sm text-slate-300">{saved}</article></div></section></main>;
}
