# Legacy Browser Support — Example 1

This app turns legacy support into an operational policy decision:

- inspect a browser capability profile
- assign a support tier
- explain whether to serve full, reduced, or unsupported UX

That is closer to the real tradeoff teams make than a static list of browser names.
