# CSS Vendor Prefixes — Example 1

This app turns the topic into a compatibility workflow:

- pick a support baseline
- inspect declarations that still need prefixed output
- compare authoring intent with generated build output

That makes the example about real browser-support policy rather than a static code snippet.
