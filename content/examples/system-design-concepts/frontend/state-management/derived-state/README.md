# Derived State Examples

- `example-1`: analytics dashboard with memoized filters, sorting, and KPI cards.
- `example-2`: selector recomputation harness showing stable vs unstable inputs.
- `example-3`: side-by-side demo of source-of-truth state vs duplicated derived state drift.
