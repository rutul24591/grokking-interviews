# Local Component State Examples

- `example-1`: project workspace app using `useState` and `useReducer` for filters, composer state, and inline edits.
- `example-2`: runnable stale-closure harness showing why functional updates are required.
- `example-3`: advanced local flow reducer covering reset, remount, and wizard-style transitions.
