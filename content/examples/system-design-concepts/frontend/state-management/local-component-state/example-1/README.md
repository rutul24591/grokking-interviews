## Local Component State - Example 1: Workspace for drafts and filters

### Run
```bash
pnpm i
pnpm dev
```

Open `http://localhost:3000` and use the workspace to filter drafts, stage edits, and inspect the reducer-driven activity log.
