# URL State Examples

- `example-1`: search and filtering app where URL query params are the canonical source of truth.
- `example-2`: codec/parser example for defaults, arrays, invalid values, and canonical URLs.
- `example-3`: back/forward synchronization check between URL state and local inputs.
