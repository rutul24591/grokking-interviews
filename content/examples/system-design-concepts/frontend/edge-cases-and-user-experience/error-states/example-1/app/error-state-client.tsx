"use client";
import { useState } from "react";
const payload = ['Search cache warmed', 'Feed rendered'];
export function ErrorStateClient() {
  const [mode, setMode] = useState<'healthy'|'retryable'|'terminal'>('healthy');
  const [attempts, setAttempts] = useState(0);
  return <main className="mx-auto min-h-screen max-w-5xl p-8"><section className="rounded-3xl border border-slate-800 bg-slate-950/70 p-6"><p className="text-sm uppercase tracking-[0.25em] text-rose-300">Error states</p><h1 className="mt-2 text-3xl font-semibold">Data panel</h1><div className="mt-6 flex flex-wrap gap-3">{['healthy','retryable','terminal'].map((value)=><button key={value} className={`rounded-2xl px-4 py-3 ${mode===value?'bg-rose-400 text-slate-950':'border border-slate-700'}`} onClick={()=>setMode(value as typeof mode)}>{value}</button>)}<button className="rounded-2xl border border-slate-700 px-4 py-3" onClick={() => setAttempts((count) => count + 1)}>Retry action</button></div><div className="mt-6 grid gap-4 lg:grid-cols-[1.1fr_0.9fr]">{mode==='healthy'?<div className="space-y-3">{payload.map((line)=><div key={line} className="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">{line}</div>)}</div>:<div className="rounded-2xl border border-rose-500/30 bg-rose-500/10 p-6 text-sm text-rose-100">{mode==='retryable'?'Temporary failure loading freshness checks. Retry to continue without losing current filters.':'This workspace is unavailable because access expired. Re-authenticate to continue.'}</div>}<aside className="rounded-2xl border border-slate-800 bg-slate-900/60 p-4"><h2 className="text-lg font-medium">Recovery context</h2><p className="mt-3 text-sm text-slate-300">Retry attempts: {attempts}</p><p className="mt-2 text-sm text-slate-400">Keep current filters, last successful data, and a concrete next action visible even when the request path fails.</p></aside></div></section></main>;
}
