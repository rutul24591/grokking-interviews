# Print Stylesheets — Example 1

This app now behaves like a real print-preview workbench:

- screen-only controls toggle print-specific content decisions
- article sections are structured to survive page breaks
- references remain meaningful when printed

That makes the topic about an actual print UX flow instead of a static list of rules.
