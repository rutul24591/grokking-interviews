# Example 1 — Server-driven feature flags (end-to-end)

Demonstrates:

- server-side evaluation (stable HTML on first render)
- targeting + percentage rollout (deterministic bucketing)
- a signed session cookie to prevent client tampering
- a kill-switch style “ops override” (simulated in-memory config)

