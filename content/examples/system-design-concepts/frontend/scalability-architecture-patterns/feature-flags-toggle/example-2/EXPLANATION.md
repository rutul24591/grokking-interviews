# Example 2 — Deterministic percentage rollout (focused)

Demonstrates:

- stable bucketing (user always maps to the same rollout bucket)
- previewing rollout impact across a list of user IDs
- why using `Math.random()` in flags is a production footgun

