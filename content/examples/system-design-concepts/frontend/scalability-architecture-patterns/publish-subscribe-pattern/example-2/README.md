## Publish-Subscribe Pattern — Example 2: Topic routing + wildcard subscriptions

This Node.js example implements a tiny in-memory pub/sub broker with:
- topic subscriptions
- `*` wildcard subscriptions

### Run
```bash
pnpm i
pnpm start
```

