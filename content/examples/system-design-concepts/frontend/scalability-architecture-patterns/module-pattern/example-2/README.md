## Module Pattern — Example 2: Module-level state and SSR leakage

This Node.js script demonstrates an edge case: module-level state is shared across “requests” inside a single process.

### Run
```bash
pnpm i
pnpm start
```

