## Factory Pattern — Example 3: Registry-based factory (plugins)

This example demonstrates an advanced variant:
- a registry where feature modules can register implementations
- runtime validation of plugin shape
- safe fallback when a key is missing

### Run
```bash
pnpm i
pnpm start
```

