## Factory Pattern — Example 2: Typed factory with compile-time exhaustiveness

This example is a small TypeScript project demonstrating:
- a discriminated union as input
- an exhaustive `switch` factory

### Run
```bash
pnpm i
pnpm build
pnpm start
```

