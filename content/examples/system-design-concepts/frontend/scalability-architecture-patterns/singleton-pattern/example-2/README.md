## Singleton Pattern — Example 2: Alternative (dependency injection)

Singletons hide dependencies. This example shows a simple alternative:
- create services explicitly
- pass them into handlers (testable)

### Run
```bash
pnpm i
pnpm start
```

