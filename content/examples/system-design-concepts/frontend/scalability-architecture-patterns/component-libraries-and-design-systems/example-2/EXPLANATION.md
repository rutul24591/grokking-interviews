# Example 2 — Typed variants helper (focused)

Demonstrates:

- a tiny `variants()` helper (like a simplified CVA) to standardize variant styling
- strong typing for `variant` and `size`
- avoiding ad-hoc string concatenation across a large codebase

