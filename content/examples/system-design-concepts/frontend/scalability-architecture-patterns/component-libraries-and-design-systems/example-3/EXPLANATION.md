# Example 3 — Design system contract audit (advanced)

Demonstrates:

- auditing rendered markup to catch regressions (missing `data-ds`, unnamed buttons)
- why design systems need “platform” quality checks, not only visual review
- isolating audit logic so it can be run in Storybook/Playwright later

