# Example 1 — Design system inside a Next.js app (end-to-end)

Demonstrates:

- tokenized styling (CSS variables) to keep UI consistent across teams
- a small component library (`Button`, `TextField`, `Card`) with accessibility defaults
- a catalog page and “real” product pages consuming the same primitives
- production patterns: `forwardRef`, stable props, and “composition over config”

