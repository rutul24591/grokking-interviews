# Example 1 — Plugin host with a stable contract (end-to-end)

Demonstrates:

- a plugin interface (id, version, capabilities, render function)
- a host that discovers + renders plugins from a registry
- enabling/disabling plugins per tenant/user (query param simulation)
- why plugin contracts are “platform APIs” that must be versioned

