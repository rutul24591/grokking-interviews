# Example 2 — Capability gating + manifest validation (focused)

Demonstrates:

- validating plugin manifests with `zod`
- enforcing capability access at the host boundary (least privilege)
- providing safe host APIs (plugin can only call what it’s allowed to call)

