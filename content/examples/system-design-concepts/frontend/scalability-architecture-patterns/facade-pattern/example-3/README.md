## Facade Pattern — Example 3: Anti-corruption layer (normalize upstream shapes)

This Node.js example demonstrates a “facade as adapter”:
- parse upstream v1/v2 shapes
- normalize to a stable internal model

### Run
```bash
pnpm i
pnpm start
```

