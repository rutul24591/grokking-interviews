## Module Federation — Example 3: Remote failure handling (fallback module)

This example focuses on an advanced concern: **remotes can be down**.

It demonstrates the idea of:
- a timeout for remote load
- a local fallback module

### Run
```bash
pnpm i
pnpm start
```

