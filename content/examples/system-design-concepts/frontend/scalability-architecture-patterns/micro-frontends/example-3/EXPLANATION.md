# Example 3 — Version negotiation + safe fallback (advanced)

Demonstrates:

- negotiating a contract version between host and remote
- falling back when the remote only supports older versions
- why “deploy independently” implies you must design for skew

