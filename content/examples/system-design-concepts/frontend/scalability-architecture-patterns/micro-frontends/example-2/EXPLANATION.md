# Example 2 — Schema-validated messaging (focused)

Demonstrates:

- validating cross-boundary messages with `zod` (treat as untrusted input)
- using `MessageChannel` to model host/remote communication without iframes
- why versioned schemas matter when teams deploy independently

