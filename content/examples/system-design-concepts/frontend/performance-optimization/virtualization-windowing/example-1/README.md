# Example 1 - Windowed List

## What it shows
- A full app rendering a 5,000-row list with manual windowing.
- Only visible rows plus overscan exist in the DOM.

## Run it
```bash
pnpm install
pnpm dev
```
