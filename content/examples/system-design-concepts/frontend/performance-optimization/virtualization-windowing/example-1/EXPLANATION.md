Example 1 is a full app demonstrating the core virtualization model.

It computes a visible range from scroll position, renders only that slice, and maintains total scroll height with a spacer container.
