Example 3 covers an advanced production decision: source-map exposure.

The script picks a source-map policy based on debugging needs versus public exposure risk, which is where minified
production debugging usually becomes operationally tricky.
