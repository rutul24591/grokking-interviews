# Example 1 - Production Build Report

## What it shows
- A build dashboard with original, minified, and gzip-transferred asset sizes.
- Source-map policy surfaced next to each optimized artifact.
- An interactive preview generated by a real `esbuild` minification step.

## Run it
```bash
pnpm install
pnpm dev
```
