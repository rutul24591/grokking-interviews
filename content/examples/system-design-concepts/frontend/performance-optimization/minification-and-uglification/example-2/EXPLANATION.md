Example 2 isolates one of the most common uglification hazards: property mangling.

It flags string-based, dynamic, and semi-private property access so you can distinguish safe mangling from cases
that require an explicit reserved-property policy.
