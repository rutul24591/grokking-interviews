Example 2 is a runnable browser demo of component-level code splitting.

The page shell and search suggestions load immediately. The expensive inspector module is downloaded only after
the user clicks the button, which is the exact pattern you want for optional workflows on an otherwise critical route.
