Example 1 is a full content page that keeps optional workflows off the initial critical path.

It demonstrates:
- `next/dynamic` for user-triggered chunk loading
- separate API requests for optional widgets
- a clear distinction between essential article content and secondary interaction surfaces
