# Example 1 - On-Demand Widget Loading

## What it shows
- A server-rendered article shell.
- Optional comments and insights panels loaded as separate chunks.
- Data fetches that only happen after the corresponding lazy chunk is opened.

## Run it
```bash
pnpm install
pnpm dev
```
