Example 3 covers a common advanced case: third-party widgets.

The script factors in third-party ownership, user intent, and bundle weight so the decision is closer to a real
production lazy-loading policy.
