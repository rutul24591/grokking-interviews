Example 2 is a focused extraction model.

It starts from a mixed stylesheet, filters to the selectors needed for the first viewport, and prints the exact rule
set you would inline into the initial document.
