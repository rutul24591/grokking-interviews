Example 3 shows the primary advanced tradeoff: once you inline too much CSS, the HTML document becomes harder to cache
and slower to send.

The script emits two HTML documents and measures how much payload growth comes purely from oversized inline CSS.
