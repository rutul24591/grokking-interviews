# Example 1 - Shared Request Dedup

## What it shows
- Multiple components consuming the same API response.
- One in-flight request shared across all consumers.

## Run it
```bash
pnpm install
pnpm dev
```
