# Example 1 - Filterable Memoized Board

## What it shows
- Derived filtering and aggregate work memoized with `useMemo`.
- Stable leaf rows guarded by `React.memo`.
- A parent-only state update that does not force every row to re-render.

## Run it
```bash
pnpm install
pnpm dev
```
