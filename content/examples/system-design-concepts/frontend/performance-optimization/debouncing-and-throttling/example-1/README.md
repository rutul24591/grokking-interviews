# Example 1 - Debounced Search Workbench

## What it shows
- A full Next.js search surface with debounced input handling.
- Abortable fetches so stale responses do not overwrite newer queries.
- Throttled telemetry separated from the user-facing search path.

## Run it
```bash
pnpm install
pnpm dev
```
