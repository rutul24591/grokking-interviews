# Example 1 - Above-the-Fold Landing Page

## What it shows
- Fast above-the-fold shell with SSR.
- Priority hero image with fixed dimensions.
- Deferred non-critical analytics and client-fetched below-fold data.

## Run it
```bash
pnpm install
pnpm dev
```

Open:
- `http://localhost:3000`
