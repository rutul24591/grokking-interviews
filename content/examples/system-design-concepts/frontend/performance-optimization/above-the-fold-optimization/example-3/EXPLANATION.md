Example 3 covers a real production guardrail: once critical CSS grows too large, the document payload itself becomes the bottleneck.

The script acts like a simple budget check and shows when aggressive inlining stops helping above-the-fold performance.
