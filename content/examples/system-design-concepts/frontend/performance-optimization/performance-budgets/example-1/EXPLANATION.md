Example 1 is a full Next.js + Node.js dashboard for production performance budgets.

It combines bundle-size and runtime-style thresholds in one view, which is how teams operationalize budgets in CI and release review.
