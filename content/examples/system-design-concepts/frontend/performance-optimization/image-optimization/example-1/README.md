# Example 1 - Optimized Editorial Image Page

## What it shows
- A hero image with high priority and stable dimensions.
- Gallery images sized for the viewport rather than the source file.
- Lightweight placeholders so content can paint before full decode.

## Run it
```bash
pnpm install
pnpm dev
```
