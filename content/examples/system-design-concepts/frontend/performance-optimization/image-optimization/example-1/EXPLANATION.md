Example 1 is a full image-delivery surface for an article page.

It demonstrates:
- `next/image` for hero and gallery delivery
- responsive `sizes` hints so smaller screens download fewer bytes
- inline placeholder SVGs to avoid empty boxes and layout shifts

This is the baseline production setup for content-heavy landing pages.
