This app now covers a broader social integration surface:

- provider-specific share URLs are built from one canonical article URL
- preview title and description are generated from backend metadata
- the UI includes a copy-link fallback for popup-blocked or unsupported provider flows
