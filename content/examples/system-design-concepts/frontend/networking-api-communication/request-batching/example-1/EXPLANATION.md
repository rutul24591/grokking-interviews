# What this example shows

Batching consolidates many small logical reads into a single physical request. This example keeps the UI flexible while avoiding origin round trips for each requested article card.
