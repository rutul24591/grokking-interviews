# What this example shows

Batching changes the failure model. One transport response can contain both successes and failures, so the client must reconcile per-item status instead of assuming all-or-nothing success.
