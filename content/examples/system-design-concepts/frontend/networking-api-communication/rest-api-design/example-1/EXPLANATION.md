# What this example shows

This app demonstrates REST fundamentals with a concrete resource model:

- collection resource: `GET /articles`
- single resource: `GET /articles/:articleId`
- nested resource creation: `POST /articles/:articleId/comments`

The goal is to show stable nouns, HTTP method semantics, and predictable status codes.
