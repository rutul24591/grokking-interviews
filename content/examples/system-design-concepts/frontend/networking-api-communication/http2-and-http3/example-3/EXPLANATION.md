# What this example shows

This check covers the non-happy-path decision: HTTP/3 is not always available or always worth it. The script codifies the common operational checks that determine whether the client should use QUIC or stay on HTTP/2.
