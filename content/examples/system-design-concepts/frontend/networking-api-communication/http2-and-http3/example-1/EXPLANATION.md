This app now runs a live streamed asset session for three protocol profiles.

- The browser measures end-to-end time while the origin emits each article asset one by one.
- `HTTP/1.1`, `HTTP/2`, and `HTTP/3` each run the same workload under different transport penalties.
- The UI updates incrementally as assets arrive, so the example is no longer just a static timing report.
