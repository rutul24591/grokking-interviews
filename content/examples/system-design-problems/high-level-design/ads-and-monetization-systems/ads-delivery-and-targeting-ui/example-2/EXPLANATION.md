# ADS Delivery AND Targeting UI - Example 2 Explanation

This example is tied to the article topic, not a generic placeholder. It models monetization entities (campaign, creative, audience, budget, ledger, policyState), telemetry (spendDriftCents, policyBlockRate, privacySuppressedRows, attributionLagMs, reconciliationGapCents), and operational actions (pause-campaign, limit-delivery, reconcile-ledger, suppress-small-cohort).

The code is meant to support interview discussion about invariants, failure boundaries, rollout safety, recovery, and the evidence needed to operate the design in production.
