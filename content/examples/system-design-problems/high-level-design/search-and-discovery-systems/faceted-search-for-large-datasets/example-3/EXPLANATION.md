# Faceted Search FOR Large Datasets - Example 3 Explanation

This example is tied to the article topic, not a generic placeholder. It models search discovery entities (query, document, facet, rankingFeature, indexShard, experiment), telemetry (zeroResultRate, indexLagMs, facetCardinality, rankScore, clickThroughRate), and operational actions (fallback-lexical, refresh-index, limit-facets, rollback-ranker).

The code is meant to support interview discussion about invariants, failure boundaries, rollout safety, recovery, and the evidence needed to operate the design in production.
