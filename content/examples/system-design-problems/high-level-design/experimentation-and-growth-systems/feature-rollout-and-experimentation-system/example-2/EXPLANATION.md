# Feature Rollout AND Experimentation System - Example 2 Explanation

This example is tied to the article topic, not a generic placeholder. It models experimentation entities (experiment, variant, assignment, exposure, metric, guardrail), telemetry (sampleRatioMismatch, exposureLagMs, guardrailDelta, cohortSize, conversionLift), and operational actions (pause-experiment, exclude-contaminated-cohort, ramp-variant, require-decision-review).

The code is meant to support interview discussion about invariants, failure boundaries, rollout safety, recovery, and the evidence needed to operate the design in production.
