# Example 3 Explanation

This example supports the article's failure-mode discussion: a previous config value should only be rolled back as a new immutable version after schema validation, and services should have explicit fallback behavior when config state is stale.
