# Example 3 Explanation

This example maps to the article's client safety, local cache, stale-while-revalidate, signed payload verification, and rollback sections. It models what a client SDK should do when config is stale, unsigned, or incompatible with its app version.
