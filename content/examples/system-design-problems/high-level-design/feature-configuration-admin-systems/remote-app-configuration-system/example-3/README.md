# Remote App Configuration System - Example 3

Concrete stale-client and rollback edge cases.

Inspect `core.ts` to see signature verification, local cache fallback, incompatible config rejection, and rollback bundle selection.
