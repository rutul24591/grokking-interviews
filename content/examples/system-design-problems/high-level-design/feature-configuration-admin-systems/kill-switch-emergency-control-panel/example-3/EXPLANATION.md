# Example 3 Explanation

This example maps to the article's runtime enforcement and recovery sections. A service checks local switch state before executing protected actions, chooses a degraded response, detects stale state, and blocks unsafe restore until acknowledgments and guardrails are healthy.
