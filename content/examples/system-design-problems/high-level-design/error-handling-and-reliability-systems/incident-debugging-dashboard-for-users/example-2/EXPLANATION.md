# Incident Debugging Dashboard FOR Users - Example 2 Explanation

This example is tied to the article topic, not a generic placeholder. It models reliability entities (incident, fallback, retryPolicy, userError, trace, recoveryStep), telemetry (errorBudgetBurn, retryAttempt, fallbackHitRate, incidentAgeMs, recoveryConfidence), and operational actions (stop-retry-storm, show-fallback, page-owner, attach-trace).

The code is meant to support interview discussion about invariants, failure boundaries, rollout safety, recovery, and the evidence needed to operate the design in production.
