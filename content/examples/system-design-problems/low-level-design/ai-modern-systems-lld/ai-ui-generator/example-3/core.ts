export interface Revision{id:string;astJson:string;validated:boolean;createdAt:number}export class PreviewRevisionStore{private revisions:Revision[]=[];private activeId?:string;add(revision:Revision){if(!revision.validated)throw new Error("unvalidated-preview");this.revisions.push(revision);this.activeId=revision.id}rollback(id:string){const target=this.revisions.find(r=>r.id===id);if(!target)throw new Error("unknown-revision");this.activeId=id;return target}active(){return this.revisions.find(r=>r.id===this.activeId)}}
export function sandboxPolicy(attrs:Record<string,string>){const blocked=Object.keys(attrs).filter(k=>/^on/i.test(k)||k==="srcdoc");return{allowed:blocked.length===0,blocked}}
export function runPreviewScenario(){const store=new PreviewRevisionStore();store.add({id:"r1",astJson:'{"type":"Text"}',validated:true,createdAt:1});store.add({id:"r2",astJson:'{"type":"Button"}',validated:true,createdAt:2});return{policy:sandboxPolicy({srcdoc:"<script/>"}),active:store.active(),rolledBack:store.rollback("r1")}}
