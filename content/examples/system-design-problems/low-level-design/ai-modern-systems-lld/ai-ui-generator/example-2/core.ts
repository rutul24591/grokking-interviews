export interface AstNode{type:string;props:Record<string,unknown>;children?:AstNode[]}const registry:Record<string,Set<string>>={Stack:new Set(["gap","direction"]),Text:new Set(["value","tone"]),Button:new Set(["label","actionId"])}
export function validateAst(root:AstNode,maxDepth=8){const errors:string[]=[];function visit(node:AstNode,depth:number){if(depth>maxDepth){errors.push("tree-too-deep");return}const allowed=registry[node.type];if(!allowed){errors.push("unsupported-component:"+node.type);return}for(const key of Object.keys(node.props))if(!allowed.has(key))errors.push("unsafe-prop:"+node.type+"."+key);for(const child of node.children||[])visit(child,depth+1)}visit(root,0);return{valid:errors.length===0,errors}}
export function runAstScenario(){return validateAst({type:"Stack",props:{gap:8},children:[{type:"Button",props:{label:"Pay",actionId:"checkout",onClick:"javascript:alert(1)"}}]})}
