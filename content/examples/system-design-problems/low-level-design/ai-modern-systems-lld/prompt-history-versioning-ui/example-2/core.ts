export interface PromptRevision{id:string;parentId?:string;body:string;model:string;createdBy:string}export class PromptRegistry{private revisions=new Map<string,PromptRevision>();private aliases=new Map<string,string>();create(revision:PromptRevision,expectedParent?:string){if(revision.parentId!==expectedParent)throw new Error("stale-base");if(this.revisions.has(revision.id))throw new Error("duplicate-revision");this.revisions.set(revision.id,Object.freeze({...revision}));return revision}promote(alias:string,revisionId:string,expectedCurrent?:string){if(this.aliases.get(alias)!==expectedCurrent)throw new Error("alias-race");if(!this.revisions.has(revisionId))throw new Error("unknown-revision");this.aliases.set(alias,revisionId)}resolve(alias:string){return this.revisions.get(this.aliases.get(alias)||"")}}
export function runPromotionScenario(){const r=new PromptRegistry();r.create({id:"p1",body:"hello {{name}}",model:"m1",createdBy:"a"});r.create({id:"p2",parentId:"p1",body:"hi {{name}}",model:"m2",createdBy:"b"},"p1");r.promote("production","p1");r.promote("production","p2","p1");return r.resolve("production")}
