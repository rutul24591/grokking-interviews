export function requiredVariables(template:string){return [...template.matchAll(/{{\s*([a-zA-Z_][\w.]*)\s*}}/g)].map(m=>m[1])}export function validateRender(template:string,values:Record<string,string>){const missing=requiredVariables(template).filter(key=>!(key in values));return{valid:missing.length===0,missing,rendered:missing.length?undefined:template.replace(/{{\s*([a-zA-Z_][\w.]*)\s*}}/g,(_,key)=>values[key])}}
export interface EvaluationGate{revisionId:string;quality:number;safety:number;costUsd:number}export function canPromote(gate:EvaluationGate){const reasons:string[]=[];if(gate.quality<.82)reasons.push("quality-regression");if(gate.safety<.98)reasons.push("safety-regression");if(gate.costUsd>.04)reasons.push("cost-budget");return{allowed:reasons.length===0,reasons}}
export function runTemplateScenario(){return{render:validateRender("Hello {{name}}, use {{product}}",{name:"Ada"}),gate:canPromote({revisionId:"p7",quality:.9,safety:.99,costUsd:.03})}}
