export interface Delta{generation:number;sequence:number;text:string;terminal?:boolean}export class ChatStreamReducer{private generation=0;private nextSequence=1;private text="";start(){this.generation++;this.nextSequence=1;this.text="";return this.generation}apply(delta:Delta){if(delta.generation!==this.generation)return{accepted:false,reason:"stale-generation",text:this.text};if(delta.sequence!==this.nextSequence)return{accepted:false,reason:"out-of-order",text:this.text};this.nextSequence++;this.text+=delta.text;return{accepted:true,terminal:!!delta.terminal,text:this.text}}}
export function runStreamRaceScenario(){const reducer=new ChatStreamReducer();const old=reducer.start();reducer.apply({generation:old,sequence:1,text:"old "});const fresh=reducer.start();return{stale:reducer.apply({generation:old,sequence:2,text:"ignored"}),fresh:reducer.apply({generation:fresh,sequence:1,text:"new",terminal:true})}}
