export interface SseEvent{event?:string;data:string}export class SseFrameParser{private partial="";private lines:string[]=[];push(text:string){this.partial+=text;const frames=this.partial.split(/\r?\n\r?\n/);this.partial=frames.pop()||"";return frames.map(frame=>this.parse(frame))}finish(){return this.partial?this.parse(this.partial):undefined}private parse(frame:string):SseEvent{let event:string|undefined;const data:string[]=[];for(const line of frame.split(/\r?\n/)){if(line.startsWith("event:"))event=line.slice(6).trim();if(line.startsWith("data:"))data.push(line.slice(5).trimStart())}return{event,data:data.join("\n")}}}
export function runSplitFrameScenario(){const parser=new SseFrameParser();return[...parser.push("data: {\"delta\":\"hel"),...parser.push("lo\"}\n\ndata: [DONE]\n\n")]}
