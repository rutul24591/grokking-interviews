export class TokenFlushBuffer{private generation=0;private queued="";private rendered="";start(){this.generation++;this.queued="";this.rendered="";return this.generation}append(generation:number,text:string){if(generation!==this.generation)return false;this.queued+=text;if(this.queued.length>50_000)throw new Error("high-watermark");return true}flush(maxChars=320){const chunk=this.queued.slice(0,maxChars);this.queued=this.queued.slice(chunk.length);this.rendered+=chunk;return{chunk,remaining:this.queued.length,rendered:this.rendered}}cancel(){this.generation++;this.queued=""}}
export function runBurstScenario(){const buffer=new TokenFlushBuffer();const stale=buffer.start();buffer.append(stale,"old");buffer.cancel();const fresh=buffer.start();return{staleAccepted:buffer.append(stale,"ignored"),freshAccepted:buffer.append(fresh,"x".repeat(700)),firstFrame:buffer.flush(),secondFrame:buffer.flush()}}
