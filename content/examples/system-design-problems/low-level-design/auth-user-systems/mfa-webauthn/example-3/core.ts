export type MfaState="idle"|"challenged"|"verifying"|"verified"|"failed";export class MfaFlow{state:MfaState="idle";generation=0;begin(){this.state="challenged";return++this.generation}verifyStart(){if(this.state!=="challenged")throw new Error("invalid transition");this.state="verifying"}complete(g:number,ok:boolean){if(g!==this.generation)return this.state;this.state=ok?"verified":"failed";return this.state}cancel(){this.generation++;this.state="idle"}}
export function runCancellationScenario(){const flow=new MfaFlow();const stale=flow.begin();flow.verifyStart();flow.cancel();const fresh=flow.begin();flow.verifyStart();return{staleResult:flow.complete(stale,true),freshResult:flow.complete(fresh,true),generation:flow.generation}}
