export interface RecoveryCode{hash:string;used:boolean}export function consumeRecovery(codes:RecoveryCode[],hash:string){const code=codes.find(x=>x.hash===hash&&!x.used);if(!code)return{ok:false,remaining:codes.filter(x=>!x.used).length};code.used=true;return{ok:true,remaining:codes.filter(x=>!x.used).length}}export class AttemptLimiter{private attempts=new Map<string,number>();allow(key:string,limit=5){const next=(this.attempts.get(key)||0)+1;this.attempts.set(key,next);return next<=limit}}
export function runRecoveryScenario(){const codes=[{hash:"a",used:false},{hash:"b",used:false}];const limiter=new AttemptLimiter();return{first:consumeRecovery(codes,"a"),replay:consumeRecovery(codes,"a"),attempts:[limiter.allow("user"),limiter.allow("user")]}}
