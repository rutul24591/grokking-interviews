import{createHash,randomBytes}from"crypto";export function pkce(){const verifier=randomBytes(32).toString("base64url"),challenge=createHash("sha256").update(verifier).digest("base64url");return{verifier,challenge,state:randomBytes(24).toString("base64url"),nonce:randomBytes(24).toString("base64url")}}export function allowedRedirect(url:string,allowed:string[]){const u=new URL(url);return allowed.includes(u.origin+u.pathname)}
