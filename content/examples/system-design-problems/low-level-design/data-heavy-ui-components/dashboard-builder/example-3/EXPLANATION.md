# Dashboard Builder — Edge Cases

Edge cases:
- Widget minimum sizes, snap-to-grid, and “drag outside bounds”
- Responsive breakpoints and per-breakpoint layouts
- Permission models: shared dashboard vs personal views

