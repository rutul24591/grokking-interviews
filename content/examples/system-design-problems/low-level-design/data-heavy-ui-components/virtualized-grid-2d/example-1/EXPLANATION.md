# Virtualized Grid (2D) — Full Implementation

Example 1 provides the core math/state for a 2D virtualized grid (rows + columns), used for dashboards and spreadsheet-like views.

Interview focus:
- Visible row/col window computation (with overscan)
- Handling variable sizes (strategy notes)
- Sticky headers/columns (offset math)

