# Column Configuration System — Full Implementation

Example 1 models user-configurable columns for tables/grids:

- Column ordering, pinning, hiding
- Widths and responsive defaults
- Versioned persistence + migration strategy

Interview focus:
- Distinguishing “schema” columns (available) vs “user layout” (chosen).
- Handling new columns introduced by backend without breaking saved layouts.

