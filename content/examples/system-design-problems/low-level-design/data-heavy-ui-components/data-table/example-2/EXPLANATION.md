# Data Table — Follow-up: Server-driven Tables

Example 2 focuses on server-driven concerns:
- Cursor pagination contracts and “jump to page” limitations
- Column-level filtering pushed to backend
- Cache keys and request deduplication
- Snapshot consistency (avoid mixing pages from different filters)

