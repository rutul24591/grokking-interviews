# Data Table — Example 1

Model + state primitives for a production-grade data table.

## Files
- `table-model.ts` — sorting/filtering/pagination pipeline
- `column-state.ts` — resize + persistence-friendly column config
- `selection.ts` — selection/shift-range logic
- `virtualization.ts` — window computation (renderer-agnostic)

