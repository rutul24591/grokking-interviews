export class Breaker{private failures=0;private openUntil=0;allow(now:number){return now>=this.openUntil}record(ok:boolean,now:number){this.failures=ok?0:this.failures+1;if(this.failures>=3)this.openUntil=now+30_000}}export function safePartial<T>(required:T,optional:T|undefined,fallback:T){return{required,optional:optional??fallback,degraded:optional===undefined}}
export function runBreakerScenario(){const breaker=new Breaker();breaker.record(false,0);breaker.record(false,1);breaker.record(false,2);return{blocked:!breaker.allow(100),partial:safePartial({cart:2},undefined,{offers:[]}),recovered:breaker.allow(31_000)}}
