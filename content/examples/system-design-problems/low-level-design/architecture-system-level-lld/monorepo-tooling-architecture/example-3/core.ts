export interface Artifact{taskKey:string;digest:string;sourceRevision:string;trusted:boolean}export class RemoteCache{private rows=new Map<string,Artifact>();write(a:Artifact,writer:"trusted"|"untrusted"){if(writer!=="trusted")throw new Error("cache-write-denied");this.rows.set(a.taskKey,Object.freeze(a))}read(key:string){return this.rows.get(key)}}
export function runCacheScenario(){const cache=new RemoteCache();cache.write({taskKey:"build|ui",digest:"sha256:a",sourceRevision:"abc",trusted:true},"trusted");let denied=false;try{cache.write({taskKey:"build|admin",digest:"sha256:b",sourceRevision:"pr",trusted:false},"untrusted")}catch{denied=true}return{hit:cache.read("build|ui"),denied}}
