export interface CommitState{generation:number;committed:number;projected:number;active:boolean;cleanup:Set<string>}export function cancel(state:CommitState,reason:string){return{...state,projected:state.committed,active:false,cleanup:new Set<string>(),reason}}export function commit(state:CommitState,generation:number){if(generation!==state.generation)return{state,accepted:false,reason:"stale-response"};return{state:{...state,committed:state.projected,active:false,cleanup:new Set<string>()},accepted:true}}
export function reconcilePersisted(state:CommitState,serverValue:number,serverVersion:number,expectedVersion:number){if(serverVersion!==expectedVersion)return{...cancel(state,"version-conflict"),serverValue};return commit(state,state.generation)}
export function runRecoveryScenario(){const state:CommitState={generation:7,committed:20,projected:68,active:true,cleanup:new Set(["pointer-capture","resize-observer","animation-frame"])};return{cancelled:cancel(state,"pointercancel"),conflict:reconcilePersisted(state,24,9,8),topic:"snap-to-grid-alignment"}}
