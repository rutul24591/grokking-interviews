export interface Rect{x:number;y:number;width:number;height:number}export function snapGrid(r:Rect,size:number):Rect{return{...r,x:Math.round(r.x/size)*size,y:Math.round(r.y/size)*size}}export function nudge(r:Rect,dx:number,dy:number,size:number,shift=false){const step=shift?size*10:size;return snapGrid({...r,x:r.x+dx*step,y:r.y+dy*step},size)}export function run(){return nudge({x:13,y:19,width:80,height:40},1,0,8)}
