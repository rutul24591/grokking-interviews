export interface Event{operationId:string;documentClock:number;expectedVersion:number;payload:string;receivedAt:number}export interface Projection{version:number;acceptedIds:Set<string>;rows:string[]}
export function applyEvent(state:Projection,event:Event){if(state.acceptedIds.has(event.operationId))return{state,accepted:false,reason:"duplicate"};if(event.expectedVersion!==state.version)return{state,accepted:false,reason:"version-gap"};if(event.payload.length>8_000)return{state,accepted:false,reason:"payload-budget"};const acceptedIds=new Set(state.acceptedIds);acceptedIds.add(event.operationId);return{state:{version:event.documentClock,acceptedIds,rows:[...state.rows,event.payload]},accepted:true}}
export function runScenario(){let state:Projection={version:4,acceptedIds:new Set(),rows:[]};const first=applyEvent(state,{operationId:"real-time-collaborative-editor-1",documentClock:5,expectedVersion:4,payload:"Apply validated operations against a document clock, isolate awareness from durable content, queue offline edits, replay missing operations before local flush, compact snapshots, and reject malicious or schema-incompatible operations.",receivedAt:100});state=first.state;return{first,duplicate:applyEvent(state,{operationId:"real-time-collaborative-editor-1",documentClock:6,expectedVersion:5,payload:"duplicate",receivedAt:110}),gap:applyEvent(state,{operationId:"real-time-collaborative-editor-3",documentClock:8,expectedVersion:7,payload:"gap",receivedAt:120})}}
