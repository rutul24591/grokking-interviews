export interface Event{participantId:string;sequence:number;expectedVersion:number;payload:string;receivedAt:number}export interface Projection{version:number;acceptedIds:Set<string>;rows:string[]}
export function applyEvent(state:Projection,event:Event){if(state.acceptedIds.has(event.participantId))return{state,accepted:false,reason:"duplicate"};if(event.expectedVersion!==state.version)return{state,accepted:false,reason:"version-gap"};if(event.payload.length>8_000)return{state,accepted:false,reason:"payload-budget"};const acceptedIds=new Set(state.acceptedIds);acceptedIds.add(event.participantId);return{state:{version:event.sequence,acceptedIds,rows:[...state.rows,event.payload]},accepted:true}}
export function runScenario(){let state:Projection={version:4,acceptedIds:new Set(),rows:[]};const first=applyEvent(state,{participantId:"live-cursor-presence-system-1",sequence:5,expectedVersion:4,payload:"Broadcast transformed document coordinates at a bounded rate. Newer cursor sequence replaces older samples, interpolation stays local, stale participants expire by TTL, and hidden tabs stop renewal. Never persist pointer trails as document history.",receivedAt:100});state=first.state;return{first,duplicate:applyEvent(state,{participantId:"live-cursor-presence-system-1",sequence:6,expectedVersion:5,payload:"duplicate",receivedAt:110}),gap:applyEvent(state,{participantId:"live-cursor-presence-system-3",sequence:8,expectedVersion:7,payload:"gap",receivedAt:120})}}
