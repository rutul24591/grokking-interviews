export interface Event{operationId:string;snapshotVersion:number;expectedVersion:number;payload:string;receivedAt:number}export interface Projection{version:number;acceptedIds:Set<string>;rows:string[]}
export function applyEvent(state:Projection,event:Event){if(state.acceptedIds.has(event.operationId))return{state,accepted:false,reason:"duplicate"};if(event.expectedVersion!==state.version)return{state,accepted:false,reason:"version-gap"};if(event.payload.length>8_000)return{state,accepted:false,reason:"payload-budget"};const acceptedIds=new Set(state.acceptedIds);acceptedIds.add(event.operationId);return{state:{version:event.snapshotVersion,acceptedIds,rows:[...state.rows,event.payload]},accepted:true}}
export function runScenario(){let state:Projection={version:4,acceptedIds:new Set(),rows:[]};const first=applyEvent(state,{operationId:"collaborative-whiteboard-1",snapshotVersion:5,expectedVersion:4,payload:"Persist semantic object operations rather than pixels. Sample strokes locally, batch points under a payload budget, order operations by document version or CRDT clock, compact snapshots, replay reconnect gaps, and keep cursor presence outside durable history.",receivedAt:100});state=first.state;return{first,duplicate:applyEvent(state,{operationId:"collaborative-whiteboard-1",snapshotVersion:6,expectedVersion:5,payload:"duplicate",receivedAt:110}),gap:applyEvent(state,{operationId:"collaborative-whiteboard-3",snapshotVersion:8,expectedVersion:7,payload:"gap",receivedAt:120})}}
