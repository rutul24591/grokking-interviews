export interface Event{eventId:string;cursor:number;expectedVersion:number;payload:string;receivedAt:number}export interface Projection{version:number;acceptedIds:Set<string>;rows:string[]}
export function applyEvent(state:Projection,event:Event){if(state.acceptedIds.has(event.eventId))return{state,accepted:false,reason:"duplicate"};if(event.expectedVersion!==state.version)return{state,accepted:false,reason:"version-gap"};if(event.payload.length>8_000)return{state,accepted:false,reason:"payload-budget"};const acceptedIds=new Set(state.acceptedIds);acceptedIds.add(event.eventId);return{state:{version:event.cursor,acceptedIds,rows:[...state.rows,event.payload]},accepted:true}}
export function runScenario(){let state:Projection={version:4,acceptedIds:new Set(),rows:[]};const first=applyEvent(state,{eventId:"real-time-notification-delivery-system-1",cursor:5,expectedVersion:4,payload:"Assume at-least-once delivery. Dedupe event ids, apply monotonic sequence cursors, detect gaps, reconnect with the last cursor, fall back to polling after bounded retries, and collapse bursty projections without losing durable records.",receivedAt:100});state=first.state;return{first,duplicate:applyEvent(state,{eventId:"real-time-notification-delivery-system-1",cursor:6,expectedVersion:5,payload:"duplicate",receivedAt:110}),gap:applyEvent(state,{eventId:"real-time-notification-delivery-system-3",cursor:8,expectedVersion:7,payload:"gap",receivedAt:120})}}
