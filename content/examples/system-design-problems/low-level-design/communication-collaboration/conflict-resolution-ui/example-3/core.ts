export interface EphemeralEntry{candidateId:string;serverVersion:number;expiresAt:number;visible:boolean}export class RecoveryStore{private entries=new Map<string,EphemeralEntry>();private replayCursor=0;renew(entry:EphemeralEntry){const prior=this.entries.get(entry.candidateId);if(!prior||entry.serverVersion>prior.serverVersion)this.entries.set(entry.candidateId,entry)}expire(now:number){for(const[key,value]of this.entries)if(value.expiresAt<=now||!value.visible)this.entries.delete(key)}observeCursor(cursor:number){if(cursor>this.replayCursor+1)return{replayFrom:this.replayCursor+1,replayTo:cursor-1};this.replayCursor=Math.max(this.replayCursor,cursor);return{replayFrom:0,replayTo:0}}snapshot(){return[...this.entries.values()]}}
export function runRecoveryScenario(){const store=new RecoveryStore();store.renew({candidateId:"a",serverVersion:2,expiresAt:5_000,visible:true});store.renew({candidateId:"a",serverVersion:1,expiresAt:9_000,visible:true});store.renew({candidateId:"b",serverVersion:1,expiresAt:500,visible:true});const gap=store.observeCursor(4);store.expire(1_000);return{gap,active:store.snapshot(),topic:"conflict-resolution-ui",recovery:"expire ephemeral state and replay durable gaps independently"}}
