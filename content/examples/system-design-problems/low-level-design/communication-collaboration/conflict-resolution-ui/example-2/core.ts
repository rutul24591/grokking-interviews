export interface Event{candidateId:string;serverVersion:number;expectedVersion:number;payload:string;receivedAt:number}export interface Projection{version:number;acceptedIds:Set<string>;rows:string[]}
export function applyEvent(state:Projection,event:Event){if(state.acceptedIds.has(event.candidateId))return{state,accepted:false,reason:"duplicate"};if(event.expectedVersion!==state.version)return{state,accepted:false,reason:"version-gap"};if(event.payload.length>8_000)return{state,accepted:false,reason:"payload-budget"};const acceptedIds=new Set(state.acceptedIds);acceptedIds.add(event.candidateId);return{state:{version:event.serverVersion,acceptedIds,rows:[...state.rows,event.payload]},accepted:true}}
export function runScenario(){let state:Projection={version:4,acceptedIds:new Set(),rows:[]};const first=applyEvent(state,{candidateId:"conflict-resolution-ui-1",serverVersion:5,expectedVersion:4,payload:"Keep base, local, and remote snapshots immutable. Produce field-level choices into a new candidate, validate the candidate, and save conditionally against the latest server version. A second conflict restarts from preserved user choices rather than silently overwriting.",receivedAt:100});state=first.state;return{first,duplicate:applyEvent(state,{candidateId:"conflict-resolution-ui-1",serverVersion:6,expectedVersion:5,payload:"duplicate",receivedAt:110}),gap:applyEvent(state,{candidateId:"conflict-resolution-ui-3",serverVersion:8,expectedVersion:7,payload:"gap",receivedAt:120})}}
