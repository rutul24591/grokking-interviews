export interface Event{participantId:string;renewalSequence:number;expectedVersion:number;payload:string;receivedAt:number}export interface Projection{version:number;acceptedIds:Set<string>;rows:string[]}
export function applyEvent(state:Projection,event:Event){if(state.acceptedIds.has(event.participantId))return{state,accepted:false,reason:"duplicate"};if(event.expectedVersion!==state.version)return{state,accepted:false,reason:"version-gap"};if(event.payload.length>8_000)return{state,accepted:false,reason:"payload-budget"};const acceptedIds=new Set(state.acceptedIds);acceptedIds.add(event.participantId);return{state:{version:event.renewalSequence,acceptedIds,rows:[...state.rows,event.payload]},accepted:true}}
export function runScenario(){let state:Projection={version:4,acceptedIds:new Set(),rows:[]};const first=applyEvent(state,{participantId:"typing-indicator-system-1",renewalSequence:5,expectedVersion:4,payload:"Emit start after a short debounce, renew at a bounded interval, stop on send or blur, and expire missing stop events locally. Aggregate labels from current TTL entries and announce only meaningful transitions to assistive technology.",receivedAt:100});state=first.state;return{first,duplicate:applyEvent(state,{participantId:"typing-indicator-system-1",renewalSequence:6,expectedVersion:5,payload:"duplicate",receivedAt:110}),gap:applyEvent(state,{participantId:"typing-indicator-system-3",renewalSequence:8,expectedVersion:7,payload:"gap",receivedAt:120})}}
