export interface SearchAutocompleteSettlement<T>{token:number;queryKey:string;value:T;}
export interface SearchAutocompleteRuntime<T>{activeToken:number;activeKey:string;focused:boolean;value?:T;evidence:string[];}
export function settleSearchAutocomplete<T>(runtime:SearchAutocompleteRuntime<T>,incoming:SearchAutocompleteSettlement<T>):SearchAutocompleteRuntime<T>{const evidence=[...runtime.evidence];if(incoming.token!==runtime.activeToken){evidence.push("stale-token-ignored");return{...runtime,evidence};}if(incoming.queryKey!==runtime.activeKey){evidence.push("stale-query-key-ignored");return{...runtime,evidence};}if(!runtime.focused){evidence.push("closed-interaction-cache-only");return{...runtime,evidence};}return{...runtime,value:incoming.value,evidence:[...evidence,"active-settlement-applied"]};}
export function planSearchAutocompleteRecovery(input:{rateLimited:boolean;streamIncomplete:boolean;citationAuthorized:boolean;urlValid:boolean}){const reasons:string[]=[];if(input.rateLimited)reasons.push("rate-limited");if(input.streamIncomplete)reasons.push("partial-stream");if(!input.citationAuthorized)reasons.push("citation-hidden");if(!input.urlValid)reasons.push("invalid-url-defaulted");return{mode:reasons.length?"degraded":"ready",reasons,api:"focus, blur, setQuery, navigate, select, retry, clearCache",structures:"query cache, debounce timer, request token, abort handle, highlighted index, focus scope, suggestion ids"};}
export function runSearchAutocompleteRecovery(){return planSearchAutocompleteRecovery({rateLimited:true,streamIncomplete:true,citationAuthorized:false,urlValid:true});}
