export interface RagBasedSearchUiSettlement<T>{token:number;queryKey:string;value:T;}
export interface RagBasedSearchUiRuntime<T>{activeToken:number;activeKey:string;focused:boolean;value?:T;evidence:string[];}
export function settleRagBasedSearchUi<T>(runtime:RagBasedSearchUiRuntime<T>,incoming:RagBasedSearchUiSettlement<T>):RagBasedSearchUiRuntime<T>{const evidence=[...runtime.evidence];if(incoming.token!==runtime.activeToken){evidence.push("stale-token-ignored");return{...runtime,evidence};}if(incoming.queryKey!==runtime.activeKey){evidence.push("stale-query-key-ignored");return{...runtime,evidence};}if(!runtime.focused){evidence.push("closed-interaction-cache-only");return{...runtime,evidence};}return{...runtime,value:incoming.value,evidence:[...evidence,"active-settlement-applied"]};}
export function planRagBasedSearchUiRecovery(input:{rateLimited:boolean;streamIncomplete:boolean;citationAuthorized:boolean;urlValid:boolean}){const reasons:string[]=[];if(input.rateLimited)reasons.push("rate-limited");if(input.streamIncomplete)reasons.push("partial-stream");if(!input.citationAuthorized)reasons.push("citation-hidden");if(!input.urlValid)reasons.push("invalid-url-defaulted");return{mode:reasons.length?"degraded":"ready",reasons,api:"submitQuestion, streamAnswer, appendCitation, inspectSource, cancel, retryRetrieval",structures:"question id, retrieval set, answer buffer, citation registry, source preview, stream cursor, confidence state"};}
export function runRagBasedSearchUiRecovery(){return planRagBasedSearchUiRecovery({rateLimited:true,streamIncomplete:true,citationAuthorized:false,urlValid:true});}
