# Design a Global API Error Handling Layer - Legacy Slug Example 1

This example keeps the old slug path concrete while the canonical article slug is global-api-error-handling. It contains the same implementation-level lifecycle, guard, retry, and telemetry behavior expected by the article.
