# Design a Rate-Limited Autocomplete System - Legacy Slug Example 2

This example keeps the old slug path concrete while the canonical article slug is rate-limited-autocomplete. It contains the same implementation-level lifecycle, guard, retry, and telemetry behavior expected by the article.
