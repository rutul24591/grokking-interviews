# Design a WebSocket Client Architecture - Example 1

This example implements a concrete heartbeat, reconnect backoff, subscription registry, and snapshot resync scenario for Design a WebSocket Client Architecture. Inspect `core.ts` for the runtime state, operation contract, guard decisions, telemetry, and edge-case handling.
