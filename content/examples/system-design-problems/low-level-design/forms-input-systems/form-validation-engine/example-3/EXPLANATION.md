# Form Validation Engine — Edge Cases: Error Shape & Accessibility

Example 3 focuses on the edge cases that make validation usable in real UIs:

- Mapping issues to stable **error codes** for analytics and localization.
- Supporting **field vs form** errors (e.g. cross-field constraints).
- Accessibility expectations: `aria-invalid`, `role="alert"`, and deterministic error ordering.

