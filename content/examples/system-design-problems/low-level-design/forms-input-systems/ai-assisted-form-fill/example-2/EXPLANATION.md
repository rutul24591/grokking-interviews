# AI-assisted Form Fill — Follow-up: Streaming UX & Cancellation

Follow-up topics:
- Streaming partial suggestions (token-by-token or field-by-field).
- Cancelling in-flight requests when user edits fields.
- Cache and reuse (avoid re-sending unchanged context).

