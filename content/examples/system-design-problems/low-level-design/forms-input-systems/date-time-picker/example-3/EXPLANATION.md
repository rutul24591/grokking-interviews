# Date/Time Picker — Edge Cases

Edge cases:
- DST transitions (missing/repeated local times).
- Keyboard navigation across months/years.
- Screen reader semantics (`role="grid"`, `aria-selected`, roving tabindex).

