# Dynamic Conditional Form Engine — Edge Cases

Example 3 covers edge cases:
- Tri-state values (`unknown` / `null`) in predicates.
- “Hidden-but-submitted” vs “cleared-on-hide” policies.
- Validation interaction: required fields becoming optional mid-flow.

