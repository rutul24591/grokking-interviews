# Form Builder — Follow-up: Schema Versioning & Migration

Follow-up topics:
- Schema changes over time (new fields, renamed fields, removed fields).
- Migrating stored drafts from version N→N+1.
- Backwards compatible rendering (server-driven forms).

