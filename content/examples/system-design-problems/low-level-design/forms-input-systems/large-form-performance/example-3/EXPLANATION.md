# Large Form Performance — Edge Cases

Edge cases interviewers look for:
- Virtualization + screen readers (ensure logical focus order).
- Restoring focus when items unmount/remount.
- Measuring field heights (variable rows) and avoiding layout thrash.

