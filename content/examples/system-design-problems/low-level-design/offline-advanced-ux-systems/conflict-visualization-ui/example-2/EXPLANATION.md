# Explanation

This example supports Design a Conflict Visualization UI. It demonstrates merge review and resolution interface, the invariant "The UI must make conflict risk legible before the user commits a destructive resolution.", and the production edge case where a user resolves one field manually while another field can be auto-merged and a third has validation errors. Use it to discuss API shape, state transitions, retry behavior, conflict handling, privacy scoping, and observability.
