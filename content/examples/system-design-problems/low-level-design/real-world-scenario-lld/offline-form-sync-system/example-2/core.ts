export interface Mutation{id:string;entity:"offline-draft";baseVersion:number;nextVersion:number;scope:string;idempotencyKey:string;payloadSize:number;authorized:boolean}export interface State{version:number;seen:Set<string>;accepted:string[]}
export function applyMutation(state:State,m:Mutation){if(state.seen.has(m.idempotencyKey))return{state,accepted:true,reason:"dedupe"};if(!m.authorized)return{state,accepted:false,reason:"unauthorized"};if(m.baseVersion!==state.version)return{state,accepted:false,reason:"version-conflict"};if(m.payloadSize>256_000)return{state,accepted:false,reason:"payload-budget"};const seen=new Set(state.seen);seen.add(m.idempotencyKey);return{state:{version:m.nextVersion,seen,accepted:[...state.accepted,m.id]},accepted:true,reason:"commit"}}
export function runMutationScenario(){let state:State={version:4,seen:new Set(),accepted:[]};const first=applyMutation(state,{id:"offline-form-sync-system-1",entity:"offline-draft",baseVersion:4,nextVersion:5,scope:"tenant-a",idempotencyKey:"k1",payloadSize:2048,authorized:true});state=first.state;return{first,duplicate:applyMutation(state,{id:"offline-form-sync-system-retry",entity:"offline-draft",baseVersion:5,nextVersion:6,scope:"tenant-a",idempotencyKey:"k1",payloadSize:2048,authorized:true}),conflict:applyMutation(state,{id:"offline-form-sync-system-stale",entity:"offline-draft",baseVersion:3,nextVersion:4,scope:"tenant-a",idempotencyKey:"k2",payloadSize:1024,authorized:true}),detail:"Persist draft data, schema version, base revision, ordered local operations, attachment references, and sync status locally. Replay idempotently after reconnect. Auto-merge independent fields, surface true conflicts, retain a recovery copy, and migrate or block drafts created under an incompatible schema."}}
