# Lightbox — Follow-up: Prefetch & Responsive Images

Follow-ups:
- Prefetch next/prev images on idle time
- Responsive sources (`srcset`, `sizes`) and DPR handling
- CDN transformations and cache keys

