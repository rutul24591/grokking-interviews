# WYSIWYG Email Builder — Full Implementation (Block Model)

Example 1 models an email builder:

- Block tree model (rows/columns/blocks)
- Drag/drop reordering intent model (no DOM coupling)
- Variable insertion tokens (`{{first_name}}`)
- Responsive preview model (desktop/mobile breakpoints)

Interview focus:
- Email client constraints (tables, inline styles)
- Sanitization and preventing XSS via templating

