# File Upload — Edge Cases

Edge cases:
- Network changes (Wi‑Fi → cellular), background tab throttling
- Large files and memory limits (streaming reads)
- Duplicate files and stable ids
- Pausing/resuming with UI feedback and timeouts

