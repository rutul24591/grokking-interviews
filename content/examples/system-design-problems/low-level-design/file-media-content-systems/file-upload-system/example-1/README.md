# File Upload System — Example 1

Queue + chunk planner + resumable state model.

## Files
- `upload-queue.ts` — concurrency + state machine hooks
- `chunk-plan.ts` — chunk splitter + progress aggregation
- `resumable-state.ts` — persistable state envelope

