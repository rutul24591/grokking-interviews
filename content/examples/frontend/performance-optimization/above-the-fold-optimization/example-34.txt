// Test across viewports for critical CSS extraction
const critical = require('critical');

critical.generate({
  src: 'https://example.com',
  dimensions: [
    { width: 375, height: 667 },   // iPhone SE
    { width: 390, height: 844 },   // iPhone 12/13
    { width: 768, height: 1024 },  // iPad
    { width: 1440, height: 900 },  // Desktop
    { width: 1920, height: 1080 }, // Large desktop
  ],
});

// Consider that LCP element may differ per viewport
// Mobile: might be a text heading (above hero image)
// Desktop: might be the hero image itself