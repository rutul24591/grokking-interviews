# Example 1 — SSR vs CSR vs ISR/Static (Next.js App Router)

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `app/[mode]/page.tsx`
- `components/CsrPanel.tsx`
- `app/api/config/route.ts`

