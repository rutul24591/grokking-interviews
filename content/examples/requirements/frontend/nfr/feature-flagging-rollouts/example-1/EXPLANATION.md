Feature flags are a rollout control plane:
- **deterministic bucketing** keeps users stable across refreshes and servers,
- **rollout percentages** reduce blast radius,
- **kill switches** provide instant rollback.

This example keeps it small but shows the key mechanics in executable form.

