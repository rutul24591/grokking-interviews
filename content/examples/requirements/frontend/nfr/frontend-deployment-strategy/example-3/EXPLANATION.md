In the context of Frontend Deployment Strategy (frontend, deployment, strategy), this example provides a focused implementation of the concept below.

Blue/green reduces risk by keeping two full environments. Real systems typically also require **stickiness** to avoid users bouncing between versions during rollout/rollback.

