# Example 1 — Abort, skeleton thresholds, optimistic UI

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `components/SearchPanel.tsx`
- `app/api/search/route.ts`
- `app/api/like/route.ts`

