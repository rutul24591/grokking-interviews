# Example 1: Legal Requests Console

Run the Next.js app:

```bash
npm install
npm run dev
```

Open `http://localhost:3000` to review legal request intake, counsel rotation, and fulfillment actions.
