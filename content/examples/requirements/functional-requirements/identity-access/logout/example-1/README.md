# Example 1 — Logout workflow

## What it shows
- Authenticated session state before logout.
- Explicit logout action that clears account session state.
- Product-visible transition back to signed-out state.

## Run
```bash
pnpm install
pnpm dev
```
