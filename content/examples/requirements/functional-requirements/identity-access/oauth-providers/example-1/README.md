# Example 1 — OAuth provider flow

## What it shows
- Start a provider redirect flow.
- Handle callback with authorization code.
- Resolve provider login into application session state.

## Run
```bash
pnpm install
pnpm dev
```
