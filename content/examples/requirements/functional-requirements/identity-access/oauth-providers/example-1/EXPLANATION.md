# Why this matters

OAuth providers are a functional requirement because the product must support delegated sign-in through external identity systems while maintaining a deterministic callback and session flow.
