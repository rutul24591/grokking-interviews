# Why this matters

Password reset is a core identity requirement because users must be able to recover account access when credentials are lost or rotated. The product needs deterministic request and completion steps.
