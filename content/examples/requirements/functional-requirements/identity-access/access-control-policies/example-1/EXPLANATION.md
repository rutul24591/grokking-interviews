# Why this matters

Access control policies are functional requirements because product behavior depends on who may perform which action on which resource. This example shows policy evaluation as a first-class workflow rather than a hidden boolean check.
