# Why this matters

Account lockout is a functional requirement because the product must visibly change authentication behavior after repeated failures. Users, support teams, and monitoring all depend on deterministic lockout rules.
