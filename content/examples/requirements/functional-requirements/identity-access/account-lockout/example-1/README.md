# Example 1 — Account lockout simulator

## What it shows
- Failed attempt tracking over a rolling threshold.
- Temporary lockout windows after repeated credential failures.
- Success-path reset behavior once a user logs in correctly.

## Run
```bash
pnpm install
pnpm dev
```
