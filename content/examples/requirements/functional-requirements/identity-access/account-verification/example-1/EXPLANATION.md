# Why this matters

Account verification is a core identity requirement because downstream features often depend on whether an account is verified. The user must see a clear verification path and predictable resend behavior.
