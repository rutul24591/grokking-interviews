# Example 1 — Credential rotation dashboard

## What it shows
- Current secret version and scheduled rotation state.
- Manual rotation workflow with new version issuance.
- Product-visible status for credentials that must be refreshed.

## Run
```bash
pnpm install
pnpm dev
```
