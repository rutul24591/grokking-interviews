# Example 1 — Authentication audit log console

## What it shows
- Login, MFA, and session revocation events captured as product-visible audit history.
- Filterable investigation workflow for support and security teams.
- Clear event metadata for actor, action, source IP, and result.

## Run
```bash
pnpm install
pnpm dev
```
