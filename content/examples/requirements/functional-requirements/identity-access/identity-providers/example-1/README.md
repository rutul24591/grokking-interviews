# Example 1 — Identity provider selector

## What it shows
- Provider selection between local auth, enterprise SSO, and external identity providers.
- Product-visible provider metadata such as domain scope and session type.
- Deterministic user flow when an account belongs to a specific provider.

## Run
```bash
pnpm install
pnpm dev
```
