# Composition pattern

In the context of Authorization Model (authorization, model), this example provides a focused implementation of the concept below.

RBAC answers “what can this role do?”.
ABAC answers “is this action allowed for this resource under these attributes?”.

Composing both avoids role explosion and keeps tenant/classification constraints explicit.

