# Edge case: conflicting rules

In the context of Authorization Model (authorization, model), this example provides a focused implementation of the concept below.

When allow and deny rules both match, define a safety policy.

Many systems use deny precedence to prevent accidental privilege escalation.

