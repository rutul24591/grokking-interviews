# Focus

In the context of Secrets Management (secrets, management), this example provides a focused implementation of the concept below.

Many leaks are operational:

- logs
- debug endpoints
- error messages

This example demonstrates a simple redaction function that can be used before logging structured objects.

