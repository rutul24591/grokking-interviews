# Focus

Some consumers pin to a schema version:

- they validate inputs against a known version
- they upgrade intentionally after testing

This example models a consumer that only accepts schemas up to a pinned version.

