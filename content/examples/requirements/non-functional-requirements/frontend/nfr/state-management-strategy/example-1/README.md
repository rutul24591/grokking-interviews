# Example 1 — Partition state: URL filters + global UI prefs + server data

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `components/Explorer.tsx`
- `lib/store.ts`
- `app/api/articles/route.ts`

