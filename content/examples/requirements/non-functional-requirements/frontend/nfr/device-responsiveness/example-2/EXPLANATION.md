In the context of Device Responsiveness (device, responsiveness), this example provides a focused implementation of the concept below.

Fluid typography reduces breakpoint churn by using `clamp(min, preferred, max)`. This demo generates `clamp()` expressions given min/max sizes and viewport range.

