In the context of Device Responsiveness (device, responsiveness), this example provides a focused implementation of the concept below.

On mobile browsers, `100vh` can include space under the URL bar, causing “jumping” layouts. Newer viewport units like `dvh` represent the *dynamic* visible area. This demo shows the difference numerically.

