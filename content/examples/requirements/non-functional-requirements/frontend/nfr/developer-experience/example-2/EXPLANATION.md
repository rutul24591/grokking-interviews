In the context of Developer Experience (developer, experience), this example provides a focused implementation of the concept below.

Structured validation errors should be easy to read in:
- terminal logs,
- dashboards,
- and UI setup screens.

This demo shows a minimal formatting approach that preserves path context.

