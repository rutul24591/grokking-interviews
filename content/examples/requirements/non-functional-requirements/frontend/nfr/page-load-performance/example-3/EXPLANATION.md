# Performance budgets as an org-scale tool

You don’t “optimize performance” once; you prevent regressions continuously.

Budgets create:
- explicit thresholds (often p75/p95),
- clear ownership (feature teams know the rules),
- and an enforcement point in CI/release.

This demo shows a minimal “p95 gate” evaluation.

