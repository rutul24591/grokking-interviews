Cross-browser compatibility is best achieved with:
- **feature detection** (capabilities),
- **progressive enhancement** (layered experience),
- and explicit **fallbacks** (polyfill vs degrade vs disable).

This example collects a small capability set and produces recommendations that a UI can present to engineers and product owners.

