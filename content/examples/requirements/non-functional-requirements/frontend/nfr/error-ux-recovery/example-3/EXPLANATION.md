In the context of Error UX Recovery (error, ux, recovery), this example provides a focused implementation of the concept below.

Circuit breakers prevent cascading failures by stopping calls to an unhealthy dependency, then probing after a cooldown (half-open). This demo simulates the state machine.

