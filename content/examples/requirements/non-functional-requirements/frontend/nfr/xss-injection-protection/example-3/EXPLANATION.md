# Edge case: escaping depends on context

Escaping rules differ across contexts. A staff-level review should validate that:
- URLs are protocol-validated,
- HTML is escaped correctly,
- and raw HTML injection is rare and audited.

