# Example 1 — Safe rich-text rendering (no HTML) + CSP defense

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `components/SafeRichText.tsx`
- `app/api/comments/route.ts`
- `next.config.ts`

