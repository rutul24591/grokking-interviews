This example shows how to do backward compatible schema evolution with explicit adapters:
- validate with `zod`
- add new fields as optional or with defaults
- keep legacy fields during deprecation windows

