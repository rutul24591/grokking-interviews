# URL state: make it explicit

URL state is a product feature:
- shareable links,
- reproducible bugs,
- and correct back/forward behavior.

Define a stable schema (and defaults) so URL parsing doesn’t become a hidden source of inconsistency.

