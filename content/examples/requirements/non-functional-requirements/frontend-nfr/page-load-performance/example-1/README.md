# Example 1 — Critical vs deferred work (Suspense streaming + lazy client widget)

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `app/page.tsx`
- `components/PerfPanel.tsx`
- `components/HeavyClientWidget.tsx`

