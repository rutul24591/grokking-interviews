# Example 1 — Virtual list (fixed row height)

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `components/VirtualList.tsx`
- `app/page.tsx`

