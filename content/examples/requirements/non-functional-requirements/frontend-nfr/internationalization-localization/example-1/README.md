# Example 1 — Locale routing + negotiation + formatting

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `middleware.ts`
- `app/[locale]/page.tsx`
- `lib/i18n.ts`

