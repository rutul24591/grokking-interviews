# Example 1 — SEO primitives in Next.js (metadata + canonical + robots + sitemap + JSON-LD)

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `app/articles/[slug]/page.tsx`
- `app/sitemap.ts`
- `app/robots.ts`

