Responsive design becomes maintainable when it’s policy-driven:
- define breakpoints/variants,
- define touch target minima,
- define media quality settings (e.g., DPR),
- and respect user preferences like reduced motion.

This example focuses on **device responsiveness** by making those decisions explicit via an API so the rules are testable and observable.
