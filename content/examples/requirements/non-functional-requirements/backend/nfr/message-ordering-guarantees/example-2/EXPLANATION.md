# Focus

In most streaming systems, ordering is guaranteed:

- within a partition / message group
- not across partitions

This example assigns keys to partitions and shows independent ordering domains.

