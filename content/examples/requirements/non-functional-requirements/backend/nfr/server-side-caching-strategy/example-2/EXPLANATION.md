# Focus

In the context of Server Side Caching Strategy (server, side, caching, strategy), this example provides a focused implementation of the concept below.

SWR serves stale data immediately and refreshes in the background:

- good for tail latency
- acceptable when data freshness isn’t strict

This example models an SWR cache decision.

