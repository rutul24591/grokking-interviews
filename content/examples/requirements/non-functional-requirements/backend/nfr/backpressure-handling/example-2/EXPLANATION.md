# Token bucket

In the context of Backpressure Handling (backpressure, handling), this example provides a focused implementation of the concept below.

Token bucket is a common primitive for:
- smoothing bursts,
- and providing predictable retry behavior.

It pairs naturally with `Retry-After` and client-side retry budgets.

