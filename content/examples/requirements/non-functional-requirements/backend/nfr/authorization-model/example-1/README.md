# Example 1 — RBAC + ABAC policy evaluation with trace

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `lib/authz.ts`
- `app/api/authz/eval/route.ts`

