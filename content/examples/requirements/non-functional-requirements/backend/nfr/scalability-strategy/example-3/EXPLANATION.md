# Focus

In the context of Scalability Strategy (scalability, strategy), this example provides a focused implementation of the concept below.

Modulo hashing (`hash(key) % N`) causes massive reshuffles when N changes.

Rendezvous hashing reduces movement during resizes.

This example compares movement percentage for the two strategies.

