# Example 1 — Version routing + deprecation headers

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `app/api/users/route.ts`
- `lib/versioning.ts`
- `lib/users.ts`

