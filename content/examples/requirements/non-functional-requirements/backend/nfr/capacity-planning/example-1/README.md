# Example 1 — Capacity calculator API + UI

## Run
```bash
pnpm install
pnpm dev
```

Agent:
```bash
pnpm agent:run -- --baseUrl http://localhost:3000
```

## Files to start with
- `lib/capacity.ts`
- `app/api/capacity/route.ts`

