# Capacity planning as a backend NFR

Capacity planning is about avoiding saturation and surprise outages by:
- estimating load (RPS, concurrency),
- translating it into compute needs,
- and adding headroom for variability and incidents.

This example uses Little’s Law and a CPU-based sizing model.

