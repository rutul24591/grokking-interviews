# Edge case: burstiness and seasonality

Average load is a trap. Capacity planning must account for:
- bursts (queueing),
- seasonality,
- and incident headroom (degraded modes).

Use real traffic distributions and validate with load tests.

