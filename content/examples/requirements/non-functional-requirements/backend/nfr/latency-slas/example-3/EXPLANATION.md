# Focus

Advanced latency control patterns:

- allocate budgets across hops (auth/db/cache)
- hedge tail latencies (send a second request after a delay)

This example models when you would hedge based on observed p95.

