# Focus

Durability also requires **integrity**:

- disks can corrupt
- partial writes can occur
- bugs can produce invalid log entries

This example demonstrates a checksum per entry to detect corruption during replay.

