# Focus

Abuse protection often requires multiple dimensions:

- per API key
- per user
- per IP / subnet
- per endpoint

This example shows how you can check multiple limiters and fail fast on the tightest constraint.

