# Focus

Traffic management often defines priority classes:

- interactive user traffic
- background jobs
- internal batch processing

Weighted fairness ensures background doesn’t starve interactive, but still makes progress.

This example models simple weighted selection.

