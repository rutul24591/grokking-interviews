# Focus

Advanced resilience techniques:

- **timeout budgets** across an entire request (don’t spend all time on one hop)
- **hedged requests** for tail latency (send a second request after a delay)

This example models a simple budget split and hedge trigger.

