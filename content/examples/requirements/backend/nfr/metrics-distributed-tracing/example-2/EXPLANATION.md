# Focus

Metrics usually export:

- counters (requests, errors)
- histograms (latency buckets)

This example shows a tiny in-memory histogram and how p95-ish approximation can be derived from buckets.

