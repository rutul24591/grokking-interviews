# Focus

For many orgs, the “right” database is a governance decision:

- choose a small set of blessed options
- document decision criteria
- prefer consistency across teams unless there’s a strong reason

This example demonstrates a tiny weighted decision matrix that produces an auditable score.

