# Focus

Availability is probabilistic.

If two independent components must both be up, availability multiplies.
If either can serve traffic (redundancy), availability improves.

This example computes simple compositions to build intuition.

