# Redaction

In the context of Centralized Logging (centralized, logging), this example provides a focused implementation of the concept below.

If you log secrets, you’ll copy them to:
- log pipelines,
- dashboards,
- and incident tickets.

Redaction should happen as close to the source as possible (logger middleware).

