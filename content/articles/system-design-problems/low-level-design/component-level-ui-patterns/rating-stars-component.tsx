"use client";
import { ArticleLayout } from "@/components/articles/ArticleLayout";import { ArticleImage } from "@/components/articles/ArticleImage";
import { HighlightBlock } from "@/components/articles/HighlightBlock";import type { ArticleMetadata } from "@/types/article";
export const metadata:ArticleMetadata={id:"article-lld-rating-stars-component",title:"Design a Rating Stars Component",description:"Implementation-heavy low-level design guide for design a rating stars component.",category:"low-level-design",subcategory:"component-level-ui-patterns",slug:"rating-stars-component",wordCount:4300,readingTime:26,lastUpdated:"2026-05-30",tags:["lld","ui-component","accessibility","principal-engineer"],relatedTopics:["focus-management","overlay-management-system","keyboard-shortcut-system"]};
export default function RatingStarsComponentArticle(){return <ArticleLayout metadata={metadata}>
<section><h1>Design a Rating Stars Component</h1><h2>Definition &amp; Context</h2><HighlightBlock as="p" tier="crucial" className="mb-4">Design a Rating Stars Component should be framed as an implementation-level design problem with a clear runtime boundary, not as a visual mock or helper function.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">Interview signal: identify the semantic DOM, controlled/uncontrolled state, accessibility, focus, lifecycle cleanup, and component API governance before discussing APIs or code structure.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">The answer should connect user-visible behavior to engineering constraints: correctness, accessibility, latency, failure recovery, testability, and operational ownership.</HighlightBlock><p>Design a Rating Stars Component is a low-level component-design problem about implementing a keyboard-operable rating input that remains predictable under keyboard, pointer, async, responsive, and accessibility pressure. The facade is focus, move, preview, commit, clear, blur. Runtime states are idle, focused, previewing, committed, disabled.</p><p>The invariant is: Preview must never overwrite the committed value until the user explicitly commits. The edge case to defend is when a user previews with pointer, then uses arrow keys and clears the committed rating. A principal-ready answer explains DOM semantics, state ownership, focus, timers, cleanup, portals, SSR, observability, and when the component should refuse an unsafe transition.</p><ArticleImage src="/diagrams/system-design-problems/low-level-design/component-level-ui-patterns/rating-stars-component-runtime.svg" alt="Design a Rating Stars Component runtime" caption="Component runtime: interaction events pass through guarded state, accessibility semantics, layout measurement, and observable cleanup." /></section>
<section><h2>Core Concepts</h2><HighlightBlock as="p" tier="crucial" className="mb-4">Core invariant: one committed semantic state must drive ARIA attributes, keyboard behavior, focus ownership, callbacks, and visual projection.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">For Design a Rating Stars Component, the strongest explanation names the state model, the data structures that hold that state, and the events allowed to mutate it.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">Do not skip ownership: distinguish product-owned state, platform-owned policy, browser/runtime state, server-authoritative state, and speculative local state.</HighlightBlock><p>Use native semantics first. Buttons, links, dialogs, lists, headings, and inputs already encode keyboard and screen-reader behavior. Add custom state only where the interaction requires it. The internal structures are value, hover preview, focused index, min, max, step, aria labels. They should remain private while the component exposes controlled and uncontrolled APIs, refs, and typed callbacks.</p><p>Separate visual state from behavioral state. Hover, focus ring, animation, image loading, and skeleton shimmer are visual projections. Open, disabled, selected, committed, active descendant, focus return, and timer pause affect behavior. Mixing them creates impossible combinations and accessibility regressions.</p><h3>Implementation contract</h3><p>Every transition should have a reason and cleanup path. Timers are cancelled on unmount. Portals release locks. Async confirmations ignore late completion after close. Keyboard movement skips disabled items. Layout measurement is batched. Reduced-motion preferences disable non-essential animation.</p><p>Support controlled mode for product-owned state and uncontrolled mode for simple usage, but never switch modes silently after mount. Expose stable ids for ARIA relationships and preserve focus across rerenders.</p></section>
<section><h2>Architecture &amp; Flow</h2><HighlightBlock as="p" tier="crucial" className="mb-4">Architecture flow should cover input event, validation, state transition, side effect, commit guard, cleanup, telemetry, and rollback.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">Key design decisions: state ownership, keyboard model, roving focus, animation policy, portals, timers, measurement, event normalization, and API surface.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">A principal-level answer should describe the hard path, not only the happy path: delayed responses, unmounts, retries, stale state, permission changes, and partial degradation.</HighlightBlock><p>The implementation has five layers: semantic root, interaction controller, state reducer, layout adapter, and accessibility adapter. The root renders stable DOM. The controller normalizes pointer, keyboard, focus, timer, and async events. The reducer accepts legal transitions. Layout work is isolated behind measurement and positioning adapters. Accessibility state derives from the committed snapshot.</p><p>A typical interaction begins with an event, validates disabled and ownership rules, commits the next state, performs layout or timer effects after commit, and publishes callbacks. On blur, escape, route change, unmount, or async settlement, cleanup verifies identity before changing state.</p><h3>Focus, timers, and ownership</h3><p>Focus ownership must be explicit. Record the opening trigger when focus should return, move focus only after the semantic node exists, and verify that the return target is still connected before restoring it. For composite widgets, use roving tab index or active-descendant patterns consistently and skip disabled items during keyboard movement.</p><p>Timers need identity and pause semantics. Delay timers, auto-dismiss timers, animation timers, and async settlement tokens should not survive disposal. Pause only when the interaction requires it, resume with the remaining duration, and avoid timer-driven state changes after the component has closed or been replaced.</p><h3>Failure and responsive behavior</h3><p>Define behavior for small viewports, zoom, long translated labels, reduced motion, virtualized parents, nested overlays, and server rendering. Avoid reading window or document during render. Hydrate with stable markup, then enhance measurement or portals after mount.</p><p>Failure states are typed: missing anchor, stale async result, invalid item id, blocked action, image load failure, timer expiry, or focus target removal. Degrade to a safe semantic baseline rather than leaving an interactive component half-open.</p><ArticleImage src="/diagrams/system-design-problems/low-level-design/component-level-ui-patterns/rating-stars-component-edge-cases.svg" alt="Design a Rating Stars Component edge cases" caption="Edge-case model: keyboard, pointer, layout, reduced-motion, async settlement, and cleanup paths remain explicit." /></section>
<section><h2>Trade offs &amp; Comparison</h2><HighlightBlock as="p" tier="crucial" className="mb-4">Trade-off lens: choose the design that keeps correctness and recovery explicit while bounding latency, memory, and integration complexity.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">Compare centralized runtime behavior with local component control. Centralization improves consistency and observability, but can become a bottleneck if extension points are not governed.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">For Design a Rating Stars Component, defend what is intentionally strict, what is configurable, and what should remain outside the abstraction.</HighlightBlock><p>Native elements are robust and accessible but less visually flexible. Fully custom widgets allow richer behavior but require keyboard, focus, ARIA, and browser testing. Prefer native semantics with composable styling, and use custom patterns only when product requirements justify the maintenance cost.</p><p>Controlled APIs improve orchestration but increase integration work. Uncontrolled APIs simplify local use but can become difficult to coordinate globally. Support both through one reducer and define ownership clearly. Portals solve clipping but complicate stacking, focus, and SSR. Animation improves comprehension but must not block interaction or layout stability.</p><p>Global coordination can simplify overlays and notifications, but it also creates hidden coupling. Use a shared manager only for truly cross-cutting concerns such as stack order, escape ownership, scroll locks, and live-region limits. Keep component-specific rendering and business actions outside the manager.</p></section>
<section><h2>Best practices</h2><HighlightBlock as="p" tier="crucial" className="mb-4">Best practice: make illegal or ambiguous states unrepresentable through explicit state unions, typed events, stable IDs, and guarded transitions.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">Test the lifecycle, not just the render output: rapid interaction, stale async settlement, unmount cleanup, keyboard-only use, SSR hydration, degraded capability, and rollback.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">Expose diagnostics that prove the design works in production: transition counts, suppressed stale work, failure reasons, cleanup counts, latency, fallback rate, and user-visible recovery.</HighlightBlock><p>Use explicit state unions, stable ids, event normalization, idempotent cleanup, selector-friendly snapshots, and semantic HTML. Test keyboard-only interaction, screen-reader attributes, pointer cancellation, focus restoration, reduced motion, high zoom, mobile viewport, long text, rapid events, and unmount during async work.</p><p>Track component errors, blocked transitions, slow layout measurement, timer drift, image failure rate, duplicate submit prevention, and accessibility regression results. Publish behavior contracts and migration notes when shared component APIs change.</p><p>Use visual regression and accessibility automation for every stable state, then add manual scripts for focus order, screen-reader announcements, mobile touch, and high zoom. Shared primitives deserve stricter gates because one regression propagates to many product surfaces.</p><h3>API governance and performance</h3><p>Keep the public API narrow. Add props only when they represent durable component policy, not one screen&apos;s styling request. Prefer composition and slots over boolean prop explosions. Mark experimental behavior explicitly, measure adoption, and publish migration notes before removing or renaming stable props.</p><p>Measure render cost, layout reads, listener count, portal count, and timer count in realistic pages. A primitive that is cheap alone can become expensive when rendered hundreds of times in a table or virtualized list. Batch measurement, avoid unnecessary context updates, and release resources as soon as the component leaves the active interaction. Record rejected alternatives in the component API review.</p><h3>Selection semantics and preview state</h3><p>Choose semantics based on the product action. A single submitted rating is a radiogroup with one radio per value. An adjustable value may use a slider when that representation is clearer. Keep committed value separate from hover or keyboard preview so pointer exit and Escape restore the accepted selection. Support a documented clear action only when zero is a valid domain value.</p><p>Render text such as “3 of 5” so meaning is not color-only. Use stable hit targets, keyboard arrows, Home, End, focus ring, and right-to-left behavior appropriate to the selected widget pattern. Optimistic persistence is reasonable for reversible ratings but needs rollback and retry evidence. Test fractional display-only ratings, disabled mode, rapid selection, server rejection, touch targets, and localization.</p></section>
<h3>Principal defense: consistency, abuse, and lifecycle rollback</h3><p>For a reusable component, consistency means one committed semantic snapshot drives DOM attributes, focus behavior, and callbacks. Pointer movement, hover previews, timers, measurements, and async settlements are transient projections. Guard every delayed effect with ownership identity so stale work cannot reopen, overwrite, or announce a component after blur, disposal, navigation, or replacement. Rollback restores the last committed semantic state and performs idempotent cleanup.</p><p>Bound work even for small widgets: cap queued notices, cached failures, measured items, portal layers, suggestion rows, and animation updates. Validate externally supplied labels, URLs, markup, dimensions, and item ids before rendering or measuring. Avoid leaking private labels or raw payloads through telemetry. Track rejected transitions, timer drift, focus-return failures, layout shifts, cleanup counts, and degraded fallbacks.</p><h3>Trade-off and privacy boundary</h3><p>The component trade-off is richer behavior versus lifecycle complexity. Add measurement, portals, caching, animation, or background work only when the interaction benefit exceeds cleanup and stale-result risk. Privacy controls matter even for small widgets: do not expose private labels, URLs, document fragments, or user activity through analytics, announcements, cached previews, or cross-scope reuse.</p><section><h2>Common Pitfalls</h2><HighlightBlock as="p" tier="crucial" className="mb-4">Main risks to call out: clickable divs, inaccessible focus order, leaked timers/listeners, stale async callbacks, layout shift, prop explosion, and broken SSR behavior.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">A common interview failure is describing the API surface but not the lifecycle guarantees that prevent stale work, leaked resources, inaccessible states, or unsafe commits.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">Do not hide failure behind generic loading and error flags. Name the difference between blocked, cancelled, stale, degraded, retrying, unauthorized, conflicted, and committed states.</HighlightBlock><p>Do not implement clickable divs when a native element exists. Do not couple visibility to focus without policy. Do not leak timers or global listeners. Do not let disabled controls fire callbacks. Do not rely on CSS alone for semantic state.</p><p>Avoid layout shifts, unbounded overlay stacks, generic z-index escalation, raw HTML rendering, and animations that resize the surrounding layout unpredictably. Ensure text wraps and controls keep stable dimensions.</p></section>
<section><h2>Real-world use cases</h2><HighlightBlock as="p" tier="crucial" className="mb-4">Real-world use: design-system primitives, form widgets, overlays, navigation, media controls, data tables, and shared enterprise UI components.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">Tie the design to operational behavior: how teams roll it out, observe it, debug it, migrate consumers, and roll it back without breaking active users.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">At staff/principal level, explain how this component or runtime reduces repeated product-team mistakes while still allowing legitimate product-specific policy.</HighlightBlock><p>These primitives appear across consumer and enterprise products. Their reliability compounds: one strong shared modal, button, tooltip, toast, or breadcrumb implementation prevents dozens of teams from repeating subtle accessibility and lifecycle mistakes.</p><p>At principal level, connect component design to design-system governance, SSR, bundle cost, browser support, test strategy, rollout, and adoption metrics.</p></section>
<section><h2>Common interview question with detailed answer</h2><HighlightBlock as="p" tier="crucial" className="mb-4">When asked to design Design a Rating Stars Component, lead with the invariant, then walk through state, events, data structures, failure handling, and measurable production signals.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">A strong answer includes a concrete edge-case walkthrough where the system receives conflicting or delayed events and still commits the correct final state.</HighlightBlock><HighlightBlock as="p" tier="important" className="mb-4">Close by naming complexity and test strategy: runtime cost, memory bounds, cleanup guarantees, accessibility tests, race tests, and observability checks.</HighlightBlock><h3>How would you implement the component?</h3><p>I would define semantic markup, reducer states, normalized events, controlled and uncontrolled ownership, refs, layout effects, accessibility attributes, cleanup, and tests.</p><h3>What edge cases matter?</h3><p>Keyboard navigation, disabled items, focus removal, viewport collision, long labels, reduced motion, stale async settlement, nested usage, and unmount cleanup. Then I would walk through a user previews with pointer, then uses arrow keys and clears the committed rating.</p><h3>What breaks at scale?</h3><p>Inconsistent APIs, accessibility drift, event-listener leaks, layout measurement storms, portal stacking conflicts, and breaking migrations. Centralize primitives and add governance and telemetry.</p><h3>How do you defend trade-offs?</h3><p>I would choose native semantics where possible, add custom behavior only for proven needs, and explain controlled ownership, SSR safety, fallback, and testing.</p></section>
<section><h2>References</h2><ul><li><a href="https://www.w3.org/WAI/ARIA/apg/" target="_blank" rel="noreferrer">WAI-ARIA Authoring Practices Guide</a></li><li><a href="https://developer.mozilla.org/en-US/docs/Web/Accessibility" target="_blank" rel="noreferrer">MDN Accessibility</a></li><li><a href="https://react.dev/learn/sharing-state-between-components" target="_blank" rel="noreferrer">React Sharing State</a></li><li><a href="https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserver" target="_blank" rel="noreferrer">MDN ResizeObserver</a></li></ul></section>
</ArticleLayout>;}
