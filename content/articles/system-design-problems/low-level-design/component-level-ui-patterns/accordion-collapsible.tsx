"use client";
import { ArticleLayout } from "@/components/articles/ArticleLayout";import { ArticleImage } from "@/components/articles/ArticleImage";import type { ArticleMetadata } from "@/types/article";
export const metadata:ArticleMetadata={id:"article-lld-accordion-collapsible",title:"Design an Accordion Collapsible System",description:"Implementation-heavy low-level design guide for design an accordion collapsible system.",category:"low-level-design",subcategory:"component-level-ui-patterns",slug:"accordion-collapsible",wordCount:4300,readingTime:26,lastUpdated:"2026-05-30",tags:["lld","ui-component","accessibility","principal-engineer"],relatedTopics:["focus-management","overlay-management-system","keyboard-shortcut-system"]};
export default function AccordionCollapsibleArticle(){return <ArticleLayout metadata={metadata}>
<section><h1>Design an Accordion Collapsible System</h1><h2>Definition &amp; Context</h2><p>Design an Accordion Collapsible System is a low-level component-design problem about implementing a accessible expandable-section controller that remains predictable under keyboard, pointer, async, responsive, and accessibility pressure. The facade is toggle, expand, collapse, expandAll, collapseAll. Runtime states are collapsed, expanding, expanded, collapsing.</p><p>The invariant is: Expansion state must remain keyboard accessible and deterministic in exclusive or independent mode. The edge case to defend is when an expanded panel changes height during animation and exclusive mode opens another item. A principal-ready answer explains DOM semantics, state ownership, focus, timers, cleanup, portals, SSR, observability, and when the component should refuse an unsafe transition.</p><ArticleImage src="/diagrams/system-design-problems/low-level-design/component-level-ui-patterns/accordion-collapsible-runtime.svg" alt="Design an Accordion Collapsible System runtime" caption="Component runtime: interaction events pass through guarded state, accessibility semantics, layout measurement, and observable cleanup." /></section>
<section><h2>Core Concepts</h2><p>Use native semantics first. Buttons, links, dialogs, lists, headings, and inputs already encode keyboard and screen-reader behavior. Add custom state only where the interaction requires it. The internal structures are item registry, expanded ids, mode, trigger ids, panel ids, animation measurement. They should remain private while the component exposes controlled and uncontrolled APIs, refs, and typed callbacks.</p><p>Separate visual state from behavioral state. Hover, focus ring, animation, image loading, and skeleton shimmer are visual projections. Open, disabled, selected, committed, active descendant, focus return, and timer pause affect behavior. Mixing them creates impossible combinations and accessibility regressions.</p><h3>Implementation contract</h3><p>Every transition should have a reason and cleanup path. Timers are cancelled on unmount. Portals release locks. Async confirmations ignore late completion after close. Keyboard movement skips disabled items. Layout measurement is batched. Reduced-motion preferences disable non-essential animation.</p><p>Support controlled mode for product-owned state and uncontrolled mode for simple usage, but never switch modes silently after mount. Expose stable ids for ARIA relationships and preserve focus across rerenders.</p></section>
<section><h2>Architecture &amp; Flow</h2><p>The implementation has five layers: semantic root, interaction controller, state reducer, layout adapter, and accessibility adapter. The root renders stable DOM. The controller normalizes pointer, keyboard, focus, timer, and async events. The reducer accepts legal transitions. Layout work is isolated behind measurement and positioning adapters. Accessibility state derives from the committed snapshot.</p><p>A typical interaction begins with an event, validates disabled and ownership rules, commits the next state, performs layout or timer effects after commit, and publishes callbacks. On blur, escape, route change, unmount, or async settlement, cleanup verifies identity before changing state.</p><h3>Focus, timers, and ownership</h3><p>Focus ownership must be explicit. Record the opening trigger when focus should return, move focus only after the semantic node exists, and verify that the return target is still connected before restoring it. For composite widgets, use roving tab index or active-descendant patterns consistently and skip disabled items during keyboard movement.</p><p>Timers need identity and pause semantics. Delay timers, auto-dismiss timers, animation timers, and async settlement tokens should not survive disposal. Pause only when the interaction requires it, resume with the remaining duration, and avoid timer-driven state changes after the component has closed or been replaced.</p><h3>Failure and responsive behavior</h3><p>Define behavior for small viewports, zoom, long translated labels, reduced motion, virtualized parents, nested overlays, and server rendering. Avoid reading window or document during render. Hydrate with stable markup, then enhance measurement or portals after mount.</p><p>Failure states are typed: missing anchor, stale async result, invalid item id, blocked action, image load failure, timer expiry, or focus target removal. Degrade to a safe semantic baseline rather than leaving an interactive component half-open.</p><ArticleImage src="/diagrams/system-design-problems/low-level-design/component-level-ui-patterns/accordion-collapsible-edge-cases.svg" alt="Design an Accordion Collapsible System edge cases" caption="Edge-case model: keyboard, pointer, layout, reduced-motion, async settlement, and cleanup paths remain explicit." /></section>
<section><h2>Trade offs &amp; Comparison</h2><p>Native elements are robust and accessible but less visually flexible. Fully custom widgets allow richer behavior but require keyboard, focus, ARIA, and browser testing. Prefer native semantics with composable styling, and use custom patterns only when product requirements justify the maintenance cost.</p><p>Controlled APIs improve orchestration but increase integration work. Uncontrolled APIs simplify local use but can become difficult to coordinate globally. Support both through one reducer and define ownership clearly. Portals solve clipping but complicate stacking, focus, and SSR. Animation improves comprehension but must not block interaction or layout stability.</p><p>Global coordination can simplify overlays and notifications, but it also creates hidden coupling. Use a shared manager only for truly cross-cutting concerns such as stack order, escape ownership, scroll locks, and live-region limits. Keep component-specific rendering and business actions outside the manager.</p></section>
<section><h2>Best practices</h2><p>Use explicit state unions, stable ids, event normalization, idempotent cleanup, selector-friendly snapshots, and semantic HTML. Test keyboard-only interaction, screen-reader attributes, pointer cancellation, focus restoration, reduced motion, high zoom, mobile viewport, long text, rapid events, and unmount during async work.</p><p>Track component errors, blocked transitions, slow layout measurement, timer drift, image failure rate, duplicate submit prevention, and accessibility regression results. Publish behavior contracts and migration notes when shared component APIs change.</p><p>Use visual regression and accessibility automation for every stable state, then add manual scripts for focus order, screen-reader announcements, mobile touch, and high zoom. Shared primitives deserve stricter gates because one regression propagates to many product surfaces.</p><h3>API governance and performance</h3><p>Keep the public API narrow. Add props only when they represent durable component policy, not one screen&apos;s styling request. Prefer composition and slots over boolean prop explosions. Mark experimental behavior explicitly, measure adoption, and publish migration notes before removing or renaming stable props.</p><p>Measure render cost, layout reads, listener count, portal count, and timer count in realistic pages. A primitive that is cheap alone can become expensive when rendered hundreds of times in a table or virtualized list. Batch measurement, avoid unnecessary context updates, and release resources as soon as the component leaves the active interaction. Record rejected alternatives in the component API review.</p></section>
<section><h2>Common Pitfalls</h2><p>Do not implement clickable divs when a native element exists. Do not couple visibility to focus without policy. Do not leak timers or global listeners. Do not let disabled controls fire callbacks. Do not rely on CSS alone for semantic state.</p><p>Avoid layout shifts, unbounded overlay stacks, generic z-index escalation, raw HTML rendering, and animations that resize the surrounding layout unpredictably. Ensure text wraps and controls keep stable dimensions.</p></section>
<section><h2>Real-world use cases</h2><p>These primitives appear across consumer and enterprise products. Their reliability compounds: one strong shared modal, button, tooltip, toast, or breadcrumb implementation prevents dozens of teams from repeating subtle accessibility and lifecycle mistakes.</p><p>At principal level, connect component design to design-system governance, SSR, bundle cost, browser support, test strategy, rollout, and adoption metrics.</p></section>
<section><h2>Common interview question with detailed answer</h2><h3>How would you implement the component?</h3><p>I would define semantic markup, reducer states, normalized events, controlled and uncontrolled ownership, refs, layout effects, accessibility attributes, cleanup, and tests.</p><h3>What edge cases matter?</h3><p>Keyboard navigation, disabled items, focus removal, viewport collision, long labels, reduced motion, stale async settlement, nested usage, and unmount cleanup. Then I would walk through an expanded panel changes height during animation and exclusive mode opens another item.</p><h3>What breaks at scale?</h3><p>Inconsistent APIs, accessibility drift, event-listener leaks, layout measurement storms, portal stacking conflicts, and breaking migrations. Centralize primitives and add governance and telemetry.</p><h3>How do you defend trade-offs?</h3><p>I would choose native semantics where possible, add custom behavior only for proven needs, and explain controlled ownership, SSR safety, fallback, and testing.</p></section>
<section><h2>References</h2><ul><li><a href="https://www.w3.org/WAI/ARIA/apg/" target="_blank" rel="noreferrer">WAI-ARIA Authoring Practices Guide</a></li><li><a href="https://developer.mozilla.org/en-US/docs/Web/Accessibility" target="_blank" rel="noreferrer">MDN Accessibility</a></li><li><a href="https://react.dev/learn/sharing-state-between-components" target="_blank" rel="noreferrer">React Sharing State</a></li><li><a href="https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserver" target="_blank" rel="noreferrer">MDN ResizeObserver</a></li></ul></section>
</ArticleLayout>;}
