"use client";

import { ArticleLayout } from "@/components/articles/ArticleLayout";
import { ArticleImage } from "@/components/articles/ArticleImage";
import type { ArticleMetadata } from "@/types/article";

export const metadata: ArticleMetadata = {
  id: "top-k-elements",
  title: "Top K Elements Pattern",
  description:
    "Bounded-heap, quickselect, and bucket-sort techniques for selecting the k best elements — the dominant pattern for streaming and frequency questions.",
  category: "other",
  subcategory: "patterns",
  slug: "top-k-elements",
  wordCount: 4500,
  readingTime: 22,
  lastUpdated: "2026-04-26",
  tags: ["top-k", "heap", "quickselect", "leetcode", "patterns"],
  relatedTopics: ["heap", "k-way-merge", "divide-and-conquer"],
};

export default function TopKElementsArticle() {
  return (
    <ArticleLayout metadata={metadata}>
      <h2 className="text-2xl font-bold mt-8 mb-4">1. Definition &amp; Context</h2>
      <p className="mb-4">
        The Top-K Elements pattern answers a single, recurring shape of question: from a collection
        of n items, return the k items that score best by some priority — largest values, smallest
        distances, most frequent words, closest points. The collection may be a static array, a
        stream of unknown length, or a virtual sequence generated by combining other structures.
        The pattern is so common that it deserves its own treatment separate from the general heap
        pattern, because the engineering choices — heap vs. quickselect vs. bucket sort — are
        worth knowing in their own right.
      </p>
      <p className="mb-4">
        Recognition signals are explicit. The question literally contains the word &quot;k&quot;:
        &quot;k largest&quot;, &quot;k smallest&quot;, &quot;k most frequent&quot;, &quot;k
        closest&quot;, &quot;k-th order statistic&quot;. There is a target priority and a
        bounded-size answer. The interviewer is testing whether you know that the global sort
        (O(n log n)) is wasteful when the answer is small, and whether you can pick the right
        among three specialised algorithms.
      </p>
      <p className="mb-4">
        The structural insight is that you do not need the global ordering. You only need to know
        the boundary between the &quot;kept&quot; and the &quot;discarded&quot; — and once you
        have that boundary, you can produce the answer in O(k) more work. Heap, quickselect, and
        bucket sort each maintain the boundary differently, with different cost trade-offs.
      </p>
      <p className="mb-4">
        The pattern matters at staff level because it doubles as a system-design micro-question.
        &quot;Top trending hashtags last hour&quot; is a top-k problem in disguise — combined with
        sliding window, sketch data structures (Count-Min, Space-Saving), and approximate
        algorithms when n is too large for exact sorting. Knowing the in-memory pattern is the
        prerequisite for the distributed conversation.
      </p>

      <h2 className="text-2xl font-bold mt-8 mb-4">2. Core Concepts</h2>
      <p className="mb-4">
        <strong>Bounded heap.</strong> A min-heap of size at most k for &quot;k largest&quot;. For
        each incoming element x: push x; if the heap size exceeds k, pop the top. The invariant is
        that the heap always contains the k largest elements seen so far, with the smallest of
        those k at the top — the next candidate to be evicted by anything bigger. The dual works
        for &quot;k smallest&quot; with a max-heap. Time O(n log k), space O(k).
      </p>
      <p className="mb-4">
        <strong>Quickselect.</strong> The selection version of quicksort. Pick a pivot, partition
        the array into less / equal / greater, and recurse only into the side that contains the
        k-th index. The total work is dominated by the first partition, giving O(n) average and
        O(n²) worst case. With randomised or median-of-medians pivot selection, the expected
        bound is rock-solid in practice. The side benefit is that after the algorithm terminates,
        the array is partitioned: the k smallest live in indices 0 through k-1 (or k largest in
        the upper segment), so you get the entire top-k as a free side effect of finding the
        k-th.
      </p>
      <p className="mb-4">
        <strong>Bucket sort by frequency.</strong> When the priority is a count and the count
        range is bounded by n, allocate buckets[0..n] and place each element in the bucket of its
        frequency. Walk from the highest bucket downward, collecting until you have k elements.
        Time O(n), space O(n). This is the optimal algorithm for top-k-frequent problems but
        requires the priority to be a small non-negative integer.
      </p>
      <p className="mb-4">
        <strong>Heap polarity rule.</strong> The heap top is the element you are willing to
        discard first. For &quot;k largest&quot;, the top is the smallest of the keepers — a
        min-heap. For &quot;k smallest&quot;, the top is the largest of the keepers — a max-heap.
        This polarity is counterintuitive and is the most common bug. Anchor on the eviction rule.
      </p>
      <p className="mb-4">
        <strong>Comparator design.</strong> When elements are tuples (frequency, word) and the
        rule is &quot;break ties by lex order&quot;, the comparator must invert one field
        relative to the other. A min-heap returns the &quot;worst&quot; first, so for top-k
        frequent words you want the heap top to be the worst keeper: smallest frequency, or on
        tie, lex-largest word. Writing the comparator on paper before code prevents 90% of bugs.
      </p>
      <p className="mb-4">
        <strong>Streaming vs. offline.</strong> Streaming forces a heap or sketch — quickselect
        cannot work without random access to the whole input. Offline favours quickselect for
        average performance and bucket sort when applicable. When the question says &quot;design a
        class with addNum and getKLargest&quot;, that is streaming and you must use a heap.
      </p>
      <p className="mb-4">
        <strong>Stability and ties.</strong> If multiple elements share the priority of the k-th,
        the question must specify which to return. Some problems pick any (215); others demand a
        deterministic order (692, 1985). Watch for the ordering clause and design the comparator
        to enforce it.
      </p>
      <ArticleImage src="/diagrams/other/leetcode/patterns/top-k-elements-diagram-1.svg" alt="Top-K pattern recognition and solution choices" />

      <h2 className="text-2xl font-bold mt-8 mb-4">3. Architecture &amp; Flow</h2>
      <p className="mb-4">
        The bounded-heap template is the universal default. Initialise an empty min-heap (for k
        largest). Iterate the input. For each element, push it into the heap; if the heap size
        exceeds k, pop the top. After the iteration completes, the heap contains the k largest
        items. If the answer must be sorted, drain the heap into a list and reverse it (the heap
        gives smallest-first, so reversing yields largest-first).
      </p>
      <p className="mb-4">
        The quickselect template requires care with partition boundaries and termination. Pick a
        pivot index in [low, high]. Partition such that elements less than the pivot value go to
        the left segment, the pivot lands at index p, and elements greater go to the right.
        Compare p to k-1 (or n-k for &quot;k largest&quot; mapped to the right side). If equal,
        return; if smaller, recurse right; if larger, recurse left. Iterative implementation
        avoids stack-overflow risk on adversarial inputs.
      </p>
      <p className="mb-4">
        The bucket-sort template is straightforward but easy to get wrong on the boundary. Build a
        frequency counter. Allocate buckets sized n+1 (so the maximum possible frequency n fits).
        For each (item, count) in the counter, append item to buckets[count]. Walk from index n
        down to 1, collecting items into a result list, returning when length reaches k.
      </p>
      <p className="mb-4">
        For top-k closest points (973), the priority is squared Euclidean distance — never use
        sqrt, both because it costs more and because it loses precision; squared distance
        preserves ordering. Push (squared_distance, point) into the heap; the comparator sorts on
        the first element naturally.
      </p>
      <p className="mb-4">
        For online top-k (703 — kth largest in a stream), the data structure exposes addNum and
        peek. Maintain a min-heap of size k. addNum pushes and pops if oversized; peek returns the
        heap top, which is the current k-th largest. The heap never grows beyond k, so each
        operation is O(log k) and memory is O(k) regardless of how long the stream is.
      </p>
      <p className="mb-4">
        For matrix or virtual-collection top-k (378, 786), the trick is to avoid materialising
        the full n² array. Push only the frontier — the next-smallest candidate from each row or
        each pair generator — and expand neighbours on pop. The heap stays O(n), and total work
        is O(k log n) instead of O(n² log n²).
      </p>

      <h2 className="text-2xl font-bold mt-8 mb-4">4. Trade-offs &amp; Comparisons</h2>
      <p className="mb-4">
        <strong>Heap vs. quickselect — average vs. worst.</strong> Heap is O(n log k) deterministic.
        Quickselect is O(n) expected but O(n²) worst. In an interview, both are acceptable; the
        choice depends on context the interviewer cares about. If they say &quot;a stream&quot;,
        heap. If they say &quot;an array, optimise expected time&quot;, quickselect. If they ask
        &quot;how does it scale to a billion?&quot;, heap, because the bounded memory footprint
        scales independently of n.
      </p>
      <p className="mb-4">
        <strong>Heap vs. sort.</strong> Sorting the whole array is O(n log n). Heap is O(n log k).
        When k = n the heap degenerates to O(n log n) — no win. The break-even is around k = sqrt(n)
        in cache-aware practice. When k is small, the heap dominates; when k approaches n, sort.
      </p>
      <p className="mb-4">
        <strong>Bucket sort vs. heap for frequency.</strong> Bucket sort is O(n) and beats the
        heap&apos;s O(n log k) when frequency is a small integer. The cost is O(n) extra memory
        for the buckets — usually fine. When the priority is a real number or a complex
        comparator, bucket sort does not apply, and heap is the answer.
      </p>
      <p className="mb-4">
        <strong>Quickselect vs. partial sort.</strong> Many languages have partial-sort utilities
        (C++ nth_element, std::partial_sort) that wrap quickselect or heap-based selection. In
        contests, prefer the library; in interviews, write the algorithm to demonstrate
        understanding.
      </p>
      <p className="mb-4">
        <strong>Top-k vs. selection algorithms.</strong> Quickselect finds the k-th order
        statistic. Top-k wants the k items above (or below) that statistic. The two are
        equivalent: once you find the k-th, the partition gives the top-k. The interviewer often
        slides between the two phrasings to test whether you notice the duality.
      </p>
      <p className="mb-4">
        <strong>Approximate top-k.</strong> When n is huge and exact answers are unaffordable
        (heavy hitters in network streams), Space-Saving or Count-Min-Sketch + heap give
        probabilistic answers in sublinear memory. This is the system-design follow-up to a top-k
        coding question — be ready to mention it.
      </p>
      <ArticleImage src="/diagrams/other/leetcode/patterns/top-k-elements-diagram-2.svg" alt="Quickselect and bounded heap compared" />

      <h2 className="text-2xl font-bold mt-8 mb-4">5. Best Practices</h2>
      <p className="mb-4">
        <strong>State the polarity invariant aloud.</strong> &quot;Min-heap of size k holds the k
        largest seen so far; top is the smallest of the keepers, evicted when something larger
        arrives.&quot; Saying it forces you to verify the polarity before writing code.
      </p>
      <p className="mb-4">
        <strong>Cap the heap on every push.</strong> The discipline is push-then-evict. If you
        ever let the heap grow past k, you have given up the entire complexity advantage.
      </p>
      <p className="mb-4">
        <strong>Randomise quickselect pivots.</strong> Never use the first or last element as
        pivot on potentially sorted input — that is exactly the worst case. Random pivot or
        median-of-three.
      </p>
      <p className="mb-4">
        <strong>Use squared distance for geometric problems.</strong> dx*dx + dy*dy beats
        sqrt(dx*dx + dy*dy) — same ordering, cheaper, exact.
      </p>
      <p className="mb-4">
        <strong>Tuple ordering for ties.</strong> In Python, push (priority, secondary, item). The
        natural tuple comparison handles tie-breakers without a custom function. In Java, write a
        Comparator that defines all keys explicitly.
      </p>
      <p className="mb-4">
        <strong>Drain at the end if order matters.</strong> If the answer must be sorted, do not
        try to read the heap array directly — drain via repeated pop into a list. The drain is
        O(k log k) which is fine for the answer phase.
      </p>
      <p className="mb-4">
        <strong>Confirm the question phrasing.</strong> &quot;K largest&quot; vs. &quot;K-th
        largest&quot; vs. &quot;K largest unique&quot; — these are different. Echo the requirement
        back to the interviewer before writing code.
      </p>

      <h2 className="text-2xl font-bold mt-8 mb-4">6. Common Pitfalls</h2>
      <p className="mb-4">
        <strong>Wrong polarity.</strong> Max-heap for &quot;k largest&quot;: the top is the
        biggest, you would never want to evict the biggest. Reverse the heap.
      </p>
      <p className="mb-4">
        <strong>Forgetting the size cap.</strong> Pushing every input element into an unbounded
        heap is O(n log n) and provides no speed-up over sorting. The cap is the entire point.
      </p>
      <p className="mb-4">
        <strong>Comparator inversion bugs.</strong> For 692, the correct order is descending
        frequency, then ascending lex. The min-heap top must be the worst — smallest frequency,
        or on tie, largest word. Double-invert one of the fields and you produce a wrong answer
        that passes some tests by accident.
      </p>
      <p className="mb-4">
        <strong>Quickselect on sorted input with first-element pivot.</strong> Adversarial inputs
        force O(n²). Always randomise.
      </p>
      <p className="mb-4">
        <strong>Off-by-one in quickselect target index.</strong> &quot;K largest&quot; means
        finding index n-k, not index k-1. Mixing the two flips the answer to &quot;k smallest&quot;.
      </p>
      <p className="mb-4">
        <strong>Floating-point ordering for sqrt distances.</strong> sqrt of two integer pairs
        can produce equal floats for distinct integer distances on some implementations. Use
        squared distances throughout.
      </p>
      <p className="mb-4">
        <strong>Bucket sort with infinite range.</strong> When frequencies can exceed n (e.g.,
        repeated stream samples) or when the priority is unbounded, bucket sort breaks. Fall back
        to heap.
      </p>

      <h2 className="text-2xl font-bold mt-8 mb-4">7. Real-World Use Cases (Canonical Leetcode)</h2>
      <p className="mb-4">
        <strong>215. Kth Largest Element in an Array.</strong> The textbook problem. Both
        quickselect (O(n) average) and min-heap of size k (O(n log k)) are accepted. Discuss both
        in interviews.
      </p>
      <p className="mb-4">
        <strong>703. Kth Largest Element in a Stream.</strong> Online variant. Maintain a
        min-heap of size k. addNum is O(log k); peek is O(1). The streaming flavour rules out
        quickselect.
      </p>
      <p className="mb-4">
        <strong>347. Top K Frequent Elements.</strong> Counter, then either min-heap of size k by
        frequency (O(n log k)) or bucket sort (O(n)). Bucket sort is the optimal answer when n is
        large.
      </p>
      <p className="mb-4">
        <strong>692. Top K Frequent Words.</strong> The tricky comparator: descending frequency,
        ascending lex. Min-heap with inverted lex on tie.
      </p>
      <p className="mb-4">
        <strong>973. K Closest Points to Origin.</strong> Squared Euclidean distance. Either
        max-heap of size k or quickselect. The interviewer often asks for both.
      </p>
      <p className="mb-4">
        <strong>378. Kth Smallest in Sorted Matrix.</strong> Two solutions — push the first
        column into a min-heap and pop k times, expanding right-neighbours, O(k log n); or binary
        search on value, count entries less or equal to mid, O(n log(max-min)).
      </p>
      <p className="mb-4">
        <strong>658. Find K Closest Elements.</strong> When the array is sorted, binary search for
        the leftmost window of size k, expanding the cheaper side at each step. O(log n + k). Heap
        works but is suboptimal.
      </p>
      <p className="mb-4">
        <strong>786. K-th Smallest Prime Fraction.</strong> Min-heap on (numerator/denominator)
        pairs, expanding neighbours. Or binary-search on value with a count function — both are
        valid.
      </p>
      <p className="mb-4">
        <strong>451. Sort Characters by Frequency.</strong> Frequency counter then bucket sort or
        max-heap by frequency. Output is the string repeated.
      </p>
      <p className="mb-4">
        <strong>1985. Find the Kth Largest Integer in the Array.</strong> Strings represent big
        integers; comparator first by length, then by lex. Tests whether you can write
        non-default comparators correctly.
      </p>
      <p className="mb-4">
        <strong>1845. Seat Reservation Manager.</strong> Min-heap of available seat IDs. reserve
        pops; unreserve pushes. Pure top-k as a data structure.
      </p>
      <ArticleImage src="/diagrams/other/leetcode/patterns/top-k-elements-diagram-3.svg" alt="Canonical top-k Leetcode problems" />

      <h2 className="text-2xl font-bold mt-8 mb-4">8. Common Interview Questions</h2>
      <ol className="list-decimal pl-6 mb-4 space-y-2">
        <li><strong>Why min-heap for k largest?</strong> Because the heap top is the eviction
        candidate. To preserve the k largest, evict the smallest of the kept ones — that is the
        min-heap top.</li>
        <li><strong>Heap vs. quickselect — which would you write here?</strong> Default to heap if
        the input is a stream or if worst-case bounds matter. Default to quickselect if the input
        is offline and you want optimal expected time.</li>
        <li><strong>Why is quickselect O(n) average?</strong> Each level partitions a strictly
        smaller portion than the previous; on average, the partition size halves, giving a
        geometric series summing to 2n. Worst case is O(n²) when the pivot is always the
        extremum.</li>
        <li><strong>How would you handle a billion items with k = 100?</strong> Bounded min-heap of
        size 100. Memory is O(k), independent of n. Streaming is the natural model.</li>
        <li><strong>What about distributed top-k?</strong> Each shard computes local top-k and ships
        the result; a coordinator merges per-shard top-k lists into a global top-k. The merge is
        a k-way merge of size-k lists.</li>
        <li><strong>Approximate top-k for huge streams?</strong> Space-Saving algorithm or
        Count-Min-Sketch keyed by item, with a heap of the heaviest counters. Sub-linear memory in
        exchange for some bounded inaccuracy.</li>
        <li><strong>How does bucket sort beat heap?</strong> When the priority is a small bounded
        integer (counts up to n), bucket sort places each item in O(1) by index — no comparison.
        Total time O(n) versus heap&apos;s O(n log k).</li>
        <li><strong>How do you break ties deterministically?</strong> Define an explicit secondary
        key in the comparator. Without it, the order is arbitrary and tests with multiple correct
        answers may still fail.</li>
        <li><strong>What if k can change after the heap is built?</strong> If k can grow, the heap
        must accept new elements once the threshold is known — keep the full sorted structure or
        switch to a balanced BST. If k can shrink, drain the heap by repeated pop until size
        equals new k.</li>
        <li><strong>Streaming top-k with deletions?</strong> Bounded heap with a tombstone or version
        counter. Lazy deletion: when an item is logically removed, mark it; when it surfaces at
        the top, discard it without using it as the answer.</li>
      </ol>
<h2 className="text-2xl font-bold mt-8 mb-4">9. References &amp; Further Reading</h2>
      <ul className="list-disc pl-6 mb-4 space-y-2">
        <li>Cormen, Leiserson, Rivest, Stein, <em>Introduction to Algorithms</em>, chapter 9
        (Medians and Order Statistics) for quickselect and median-of-medians; chapter 6 for
        heap-based selection. Sedgewick and Wayne, <em>Algorithms</em>, 4th ed., section 2.5
        (Applications) covers top-k and selection.</li>
        <li>Leetcode tag &quot;heap (priority queue)&quot; lists every top-k problem in the canon.
        Grokking the Coding Interview&apos;s &quot;Top K Elements&quot; pattern provides a curated
        set of 8–10 problems. NeetCode 150 dedicates a section to top-k with detailed walkthroughs.</li>
        <li>For the streaming literature: Metwally, Agrawal, El Abbadi, &quot;Efficient Computation of
        Frequent and Top-k Elements in Data Streams&quot; (Space-Saving), 2005. Cormode and
        Muthukrishnan, &quot;An Improved Data Stream Summary: The Count-Min Sketch and its
        Applications&quot;, 2005.</li>
      </ul>
    </ArticleLayout>
  );
}
